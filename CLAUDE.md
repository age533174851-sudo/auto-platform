# TRAIGO — 최상위 규칙

> **사용자는 명령만 한다.**
>
> 기술적으로 자동화 가능한 수동 절차를 사용자에게 넘기지 않는다.
> "사용자가 해야 할 것"이 발생하면 **먼저 그것을 자동화하는 기능을 구현한다.**
> 최초 권한 연결과 실제 자금에 대한 명시적 승인 외의 일상 운영 수동 작업은
> 0을 목표로 한다.

사용자가 하는 말은 최종적으로 이 정도만 남아야 한다:

```
전체 점검해 · 배포해 · 테스트넷 검증해 · 이 전략 켜 · 지금 중지해 · 복구해 · LIVE_SMALL 승인
```

## 작업 보고에 이런 문장이 나오면 자동화 미완성이다

- "마이그레이션을 적용해 주세요"
- "Vercel 환경변수를 확인해 주세요"
- "Fly secret을 맞춰 주세요"
- "fly logs를 열어 확인해 주세요"
- "SHA를 비교해 주세요"
- "heartbeat를 확인해 주세요"
- "WORKER_PROVIDER를 수동으로 넣어 주세요"
- "exit-monitor가 도는지 사람이 봐 주세요"

이런 문장이 필요해지면 **그 문장을 없애는 코드를 먼저 쓴다.**
보고에는 "사용자가 해야 할 것"이 아니라 **"시스템이 자동으로 한 것 /
자동 복구하지 못한 이유"**만 남는다.

## 자동화보다 실패 지점 제거가 우선이다

체크리스트를 자동으로 눌러 주는 것보다 **체크리스트 자체를 없애는 것**이
낫다.

> `EXIT_MONITOR_SECRET`을 `ADMIN_SECRET`과 맞추는 일을 자동화하기보다,
> exit-monitor를 Worker 안으로 옮겨 **두 서비스 간 secret 일치라는 실패
> 지점 자체를 없앤다.**

새 자동화를 만들기 전에 항상 먼저 묻는다: 이 단계가 아예 없어질 수는 없는가.

## 사람이 남는 세 가지 예외

1. **최초 한 번** 외부 서비스 권한 연결 (토큰을 시스템에 연결)
2. 새 API 키·토큰 **자체의 발급**
3. LIVE 자금·파괴적 DB 변경처럼 **명시적 승인이 필요한 결정**

이 셋도 대시보드를 여러 곳 돌아다니게 하지 않는다. 권한을 한 번 연결한
뒤에는 **승인 명령 하나**로 시스템이 수행한다.

## 절대 규칙 (기존)

- **Cancel All 금지.** 다른 전략의 보호주문을 지우지 않는다.
- **symbol만 보고 주문 소유권을 판단하지 않는다.**
- **UNKNOWN을 0으로 적지 않는다.** 확인하지 못한 것은 통과가 아니다.
- **MOCK / TESTNET / LIVE의 장부와 자산을 절대 합산하지 않는다.**
- **`my-original-v1` 원본 전략은 덮어쓰거나 삭제하지 않는다.**
- 실제 거래소 증거 없이 `ENTERED` · `CLOSED` · `PROTECTED` · `PASS`를 적지 않는다.
- 사용자 확인 없이 포지션·체결 이력을 지우지 않는다.
- 사용자가 만든 전략의 레버리지·TP/SL·진입조건을 자동으로 낮추지 않는다.
- 검사 실패 상태에서의 강제 머지 기능은 만들지 않는다.
- **키·시크릿 값은 로그·응답·커밋 어디에도 남기지 않는다.** 지문만 비교한다.
- LIVE는 모든 TESTNET 검증이 끝나기 전에 활성화하지 않는다.

## 자주 나는 고장 두 가지

1. **만들어 놓고 배선을 안 함** — 기능은 있는데 화면·경로에 연결이 없다.
2. **경로가 둘인데 한쪽만 고침** — 같은 판단이 두 곳에 있으면 언젠가 갈린다.

새 판단 로직은 한 곳에 두고 테스트를 붙인다. 스크립트가 앱과 같은 판단을
해야 하면 **로직을 복제하지 말고 같은 파일을 컴파일해서 쓴다**
(`scripts/gen-migration-manifest.mjs`의 `loadPlan()`이 그 예다).
