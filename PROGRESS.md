# TRAIGO 작업 현황

> 대화가 길어져 컨텍스트가 요약될 수 있으므로 남기는 인수인계 문서.
> 마지막 갱신: 2026-07-30
>
> **이 문서를 고칠 때**: 숫자(테스트 수·타입 에러·마이그레이션 번호)는 기억이 아니라
> 실행 결과를 적는다. 낡은 숫자는 없는 것보다 나쁘다 — 이 문서가 한 번 그랬다.
> 테스트 수가 55로 적혀 있는 동안 실제로는 528개였고, 019·020 마이그레이션은
> 적용 필요 목록에 아예 없었다.

## 지금 상태 한눈에

**돌아가는 것**: 신뢰성 기반 5가지 · PC/모바일 터미널 · 빠른 명령 팔레트(Ctrl+K, 초성 검색) ·
시장 분리(현물·USDⓈ-M·COIN-M) ·
현물 전략 10종 + 결합 전략 6종 · 뉴스·AI·캘린더 정보 계층 · 실시간 호가창

**막혀 있는 것**: 마이그레이션 3건(018·019·020) + 시크릿 미설정.
019가 없으면 뉴스가 저장되지 않고, `ADMIN_SECRET`이 없으면 청산 감시가 401이다.
**테스트넷 가동(다음 단계 A)이 여기서 멈춰 있다.**

**제일 위험한 것**: Gate 거래소 경로에 마진타입 설정과 손절 부착이 없다.
바이낸스 경로에서 막아둔 것들이 거기는 뚫려 있고, 이건 실제 돈이 걸린 경로다.

| 검증 | 값 (2026-07-30 실행) |
|---|---|
| 유닛 테스트 | 621 통과 / 0 실패 |
| 타입 에러 | 80건 (`ignoreBuildErrors`로 덮여 있음) |
| 프로덕션 빌드 | 통과 · 미들웨어 27.6 kB |

## 인프라

| 항목 | 상태 |
|---|---|
| Vercel | `auto-platform-zeta.vercel.app` · regions `hnd1`(도쿄) · Hobby 플랜 |
| GitHub | `age533174851-sudo/auto-platform` · main 브랜치 |
| Supabase | ref `sgbysrvvxlluzffmgcho` · 39개 테이블 |
| Railway 워커 | **사용 안 함** — Binance가 IP 지역 차단 (`Service unavailable from a restricted location`) |

**Binance 도달성**: Vercel(hnd1)에서는 선물·현물·테스트넷 모두 정상. Railway만 차단됨.
그래서 청산 감시를 Vercel Cron + GitHub Actions로 옮겼다.

## 아직 안 된 설정 (사용자 작업)

코드로는 뚫을 수 없는 것들이다. **이 중 마이그레이션과 시크릿이 안 되면 다음 단계 A
(테스트넷 가동)는 시작 자체를 못 한다.**

### 마이그레이션 3건 미적용

Supabase SQL 편집기에 파일 내용을 붙여넣고 실행. (관리 API 토큰이 환경에 없어
자동 적용을 못 했다.)

- [ ] `018_live_orders_recovery.sql` — 적용 전에도 앱은 돌지만 UNKNOWN 주문의
      포지션 교차 확인이 생략된다. 새 컬럼을 빼고 다시 쓰는 폴백이 돈다
- [ ] `019_news_articles.sql` — **없으면 뉴스 파이프라인이 아무것도 저장하지 못한다**
- [ ] `020_ai_usage_detail.sql` — 없으면 AI 비용·응답시간 관리센터가 빈 표로 뜬다

### 시크릿·환경변수

- [ ] `ADMIN_SECRET` — Vercel 환경변수 + GitHub Secret(`EXIT_MONITOR_SECRET`)에 동일 값
      없으면 청산 감시 cron이 401로 실패한다
- [ ] GitHub Secret `EXIT_MONITOR_URL` = `https://auto-platform-zeta.vercel.app`
      (뉴스 파이프라인 워크플로도 이 값을 쓴다)
- [ ] GitHub Secret `NEWS_CRON_SECRET` = Vercel의 `CRON_SECRET`과 같은 값
      없으면 뉴스 수집·캘린더·AI 분석·정리 4단계가 전부 401
- [ ] 뉴스 공급자 키(`FINNHUB_API_KEY` 등) + AI 키(`OPENAI_API_KEY` 등)를 Vercel에
      없으면 각 단계가 **503으로 그 사실을 알려준다** (조용히 빈 값을 넣지 않는다)

### 외부 발급이 필요한 것

- [ ] 바이낸스 테스트넷 연결 재등록 (암호화 키가 바뀌어 기존 항목은 못 읽음)
- [ ] Google OAuth 클라이언트 ID/비밀 — 로그인 실패의 원인. 아래 '미처리 UI' 참조
- [ ] 대화 초반 노출된 Supabase/Vercel 토큰 폐기

## 실데이터 검증 결과 (핵심)

09:00 KST 전환 → N분 관찰 → 1회 진입 → 다음 09:00 청산 전략.
Mark Price 1분봉 기준, 비용(수수료 0.08% + 슬리피지 0.03% + 펀딩 0.01%×3) 반영.

| 심볼 | 기간 | MAE 중앙값 | 100배 생존률 | 100배 기대값 |
|---|---|---|---|---|
| BTCUSDT | 90일 | 1.29~1.49% | 18~21% | -77~-82% |
| BTCUSDT | 365일 | 1.28~1.36% | 17~21% | -78~-80% |
| ETHUSDT | 365일 | 1.75~1.98% | 14~18% | -80~-85% |

**결론: 구조적이다.** 기간을 4배로 늘리고 심볼을 바꿔도 동일.
MAE 중앙값이 100배 청산거리(0.5%)의 **2.6~4배**다.
방향을 맞혀도 23시간 30분 보유 중 중간 역행에서 청산된다.

**단, 방향 적중률(41~52%)은 사용자 실력이 아니라 검증 스크립트의 임의 판단 규칙 성적이다.**
(몸통 30% 이상 + 반대꼬리가 몸통보다 짧으면 진입)
MAE 분포는 진입 규칙과 무관하므로 그 결론만 유효하다.

재현: `npm run validate:real` (일반) / `node scripts/validate-09kst.mjs BTCUSDT 365` (09시 전략)

## 다음 단계 (사용자 합의 순서)

- [x] **D** 교차 확인 — BTC 365 + ETH 365 완료
- [ ] **A** 테스트넷 가동 — **막혀 있다.** `ADMIN_SECRET` + 마이그레이션 018이 먼저다
      (위 '아직 안 된 설정'). 시작되면 하루 1표본씩 누적
- [x] **B** 비상 종료 — `positionGuard.ts` 작성 + `/api/autotrade/exit-monitor`에 배선 완료
- [ ] **C** 판단 규칙 개선 후 재측정 — **사용자의 실제 진입 기준을 받아야 시작할 수 있다.**
      현재 측정에 쓴 규칙(몸통 30% + 반대꼬리)은 내가 임의로 정한 것이다

A와 C 둘 다 코드가 아니라 입력을 기다리는 상태다. A는 시크릿, C는 진입 기준.



## 진행 중 / 확인 필요

### ✅ 실시간 호가창 — 완료 (프로덕션 확인됨)
`Math.random()` 가짜 호가를 Binance 실시간 데이터로 교체했다.
프로덕션(auto-platform-zeta.vercel.app)에서 확인:
```
가격 (USDT) | ● 실시간 | 수량
63,314 | 0.002  …  63,316 USDT ▼2.66%  …  63,313 | 17.599
호가 잔량 매수 95.8% | 4.2% 매도
```

거쳐온 문제 3가지 (재발 시 참고):
1. **심볼 슬래시** — `sel.sym`이 'BTC/USDT'라 `.replace(/USDT$/,'')+'USDT'`가
   'BTC/USDT'를 만들어 없는 스트림에 붙었다. Binance는 연결만 받아주고 데이터를
   안 보내서 "● 실시간인데 비어 있음"이 됐다. 영숫자만 남기도록 수정.
2. **단위 혼입** — 현재가가 stream.lastPrice 없을 때 원화(sel.p)로 폴백해
   호가는 63,654(USDT)인데 중앙만 94,061,179(KRW)가 됐다. 최우선 호가로 채우고
   'USDT' 명시.
3. **스트림 지역 제한** — Node로 측정한 결과 (브라우저 무관하게 재현):
   depth20 정상 / bookTicker 1737건 / **aggTrade·ticker·markPrice·kline 모두 0건**.
   현물 aggTrade는 80건 옴. 코드 버그가 아니라 접속 지역 제한이다.
   → 현재가는 bookTicker 중간값, 변동률은 REST /fapi/v1/ticker/24hr 5초 폴링,
     매수/매도 강도는 체결 대신 **호가 잔량**(bookImbalance)으로 대체.
     화면 표기도 '호가 잔량 매수'로 바꿔 체결 강도와 구분했다.
   ※ 현물 aggTrade는 오지만 쓰지 않았다 — 선물 호가에 현물 체결을 섞으면
     서로 다른 시장 값이 한 화면에 놓이고 사용자가 구분할 방법이 없다.
   ※ WebSocket은 사용자 브라우저에서 직접 Binance로 붙으므로, 이 제한은
     서버 리전이 아니라 접속자 네트워크 기준이다. 지역이 다르면 ticker가
     정상적으로 올 수도 있다 — 그 경우에도 현재 구조는 문제없다(오는 것만 씀).

확인 절차: 매매 → 상단 서브탭 '매매'(y≈206, 하단 네비 아님) → 코인 선택
### 사용자가 지적한 미처리 UI
- ~~하단 `더보기` 메뉴~~ ✅ 해결 (`5b28524`). 원인은 내가 넣은 모달 뒤로가기였다:
  주소 동기화가 `pushState`로 탭을 쌓고, 모달 정리가 그걸 `history.back()`으로
  되감아 눌러도 제자리였다. 두 곳에 같은 기준(맨 위가 모달 표식인가)을 넣어 고쳤다.
  브라우저에서 백테스트·뉴스·포트폴리오 3항목 이동 확인.
- Google 로그인 실패 — Supabase `external_google_enabled = False`.
  **활성화하려면 Google Cloud OAuth 클라이언트 ID/비밀번호가 필요하다 (사용자 발급 필수).**
  리디렉션 URI: `https://sgbysrvvxlluzffmgcho.supabase.co/auth/v1/callback`
- 로컬 개발용 `.env.local`은 생성 완료 (Supabase URL/anon/service 키)

### 요청받은 화면 스펙 (스크린샷 제공됨)
Binance Futures 스타일 — PC 3열(차트 | 호가+체결 | 주문폼), 모바일 세로 스택.
사용자가 스크린샷 3장을 제공했다. 반영 시 그 구조를 따를 것.

## 매매 화면 개편 (일부 완료)

사용자가 요청한 우선순위:
1. ~~거래소-앱 포지션 자동 대조~~ ✅ **완료** — 신뢰성 1번(`stateReconcile.ts`)이 이걸
   덮었다. `GET /api/reconcile/state` + 주문 직전 409 차단 + 화면 배너
2. 계좌 위험판 — **미착수.** `calcLiquidationPrice`, ISOLATED 검사, 킬스위치 재사용
3. 포지션/미체결 통합 관리 — **일부.** 포지션 카드와 시장가 청산은 됐고
   (`positionView.ts`), 미체결 통합은 남았다
4. ~~실시간 호가·체결~~ ✅ 완료 (위 참조)
5. 차트 위 TP·SL 드래그 — **미착수.** 코드에 흔적 없음을 확인했다

별도로 09시 전략 전용 화면 7단계 명세도 받아둠 (관찰 패널 · LONG/SHORT 판정판 ·
100배 생존 패널 · 주문 확인 · 포지션 보유 · 09시 교체).

## 감사 지적 처리 현황

| # | 내용 | 상태 |
|---|---|---|
| 1 | 웹훅 connectionId 위조 | ✅ user_id 소유권 검사 추가 (근본 해결은 사용자별 토큰) |
| 2 | ISOLATED 미강제 | ✅ `/fapi/v1/marginType` 호출 추가, 실패 시 주문 중단 |
| 3 | 손절 실패인데 진입 성공 | ✅ 즉시 청산 + ok:false |
| 4 | 워커 lock 중복 주문 | ✅ 락 no-op 제거 · stale 회수 조건 강화 · clientOrderId 멱등 |
| 5 | Expansion 미적용 | ✅ 주문 경로 연결 + 상한 5→100 개방 |
| 6 | TP/SL 수정이 타 주문 취소 | ✅ closePosition=true인 STOP/TP만 선별 취소 |
| 7 | DB 컬럼명 불일치 | ✅ `api_key_enc`(없는 컬럼) → `api_key`/`api_secret_enc` |
| 8 | reconcile 무인증 | ✅ 관리자 JWT 또는 x-admin-secret 요구 |
| 9 | 캘린더 아무나 변조 | ✅ `checkAdminRole`로 관리자 전용 |
| 10 | 전략 중복(10슬롯 vs 하루1회) | ❌ 미처리. `slotManager.ts`는 폐기 표시만 되어 있음 |

추가 발견: `/api/safety`에 하드코딩 관리자 시크릿(`traigo-admin-dev`) → 제거 완료

## 미처리 과제

위험한 순서로 적는다.

1. **Gate 거래소 경로**: 마진타입 설정 없음, 손절 부착 없음,
   `setLeverageGateFutures` 결과 미확인. 바이낸스 경로에서 막아둔 것들이 여기는 뚫려 있다.
2. **타입 에러 80건** — `next.config.js`가 `ignoreBuildErrors: true`로 덮고 있다
3. `slotManager.ts` 정리 (감사 10번). 폐기 표시만 되어 있고 파일은 남아 있다:
   `src/lib/strategies/slotManager.ts`
4. 신뢰성 2번 잔여: 주문 생명주기를 **화면에 표시**, UNKNOWN 주문의 자동 조회 확정

~~`middleware.ts`가 `/admin`·`/developer`를 막지 않는다~~ ✅ 해결 (아래 절 참조)

## 추가 요청 기능 (사용자 제시, 미착수)

플랫폼 완성도 항목 — 사용자가 꼽은 상위 5개를 먼저:
1. **매매 리플레이** — 과거 날짜 선택 → 09시 전환·관찰·가상 진입·24시간 결과 재생
2. **전략별 독립 장부 + 충돌 관리** — 장부는 `lib/strategies/ledger.ts`로 **완료**
   (자기 귀속분까지만 청산, 차이는 '미귀속'으로). **남은 것은 충돌 시 정책 선택 UI**
3. **거래 전 자동 체크리스트** — ISOLATED/배율/기존포지션/오늘진입/시간동기화/청산거리 점검 후 주문 잠금
4. **계좌 스냅샷·장애 복구** — 09시와 주문 전후 자동 저장, 재시작 시 거래소와 대조 복구
5. **수익 보호 금고** — 보호 수익을 증거금 계산에서 제외, 목표 도달 시 새 사이클 자동 생성

나머지: ~~실시간 데이터 품질 표시~~ · ~~기능 신뢰도 표시(실시간/지연/모의/AI추정 구분)~~
→ 둘 다 신뢰성 4번(`dataQuality.ts`)으로 **완료**. 나머지는 미착수:
주문 실행 품질 분석, 사고 대응 화면, 성과 원인 분석(요일·관찰시간·첫봉형태별),
매매 비교 실험실, 고급 알림 규칙, 워크스페이스 저장, 내역 내보내기(CSV/PDF).

신뢰도 표시가 특히 중요했던 이유 — 호가창이 가짜 데이터를 보여주던 것이 실제 사례였고,
그 뒤 `useLivePrices`가 **모든 종목 가격을 난수보행 값으로 공급하던 것**도 같은 종류였다.
둘 다 화면에서는 정상으로 보였다.

## PC 터미널 UI — 1차 완료 (`/terminal`)

`components/terminal/` — Shell · Context · TopBar · LeftRail · ChartPane · OrderPane · BottomDock

**실측 확인** (1600×1080 기준, 브라우저에서 직접 측정):
| 항목 | 스펙 | 실측 |
|---|---|---|
| 좌 | 18% | 17.9% (287px) |
| 중앙 | 55% | 54.7% (876px) |
| 우 | 27% | 26.9% (430px) |
| 하단 | 25~30% | 26.4% (238px) |
| 상단 | 얇게 | 46px |

- **상태 격리 검증됨**: 좌측 탭을 시장→뉴스→일정→AI로 4번 돌려도
  차트 iframe이 **같은 DOM 노드**로 유지된다 (sameNode: true, src 불변).
  뉴스가 갱신될 때마다 차트가 리셋되면 못 쓰는 화면이 된다.
- **심볼 동기화 검증됨**: 상단에서 ETH로 바꾸면 차트·주문버튼·수량 placeholder가
  한꺼번에 따라오고 localStorage에 저장된다.
- 주문 버튼 415×46, Kill Switch 106×44 — 크게 유지. 고밀도는 뉴스·시장 목록에만.
- 실자금 모드면 상단 띠가 붉어지고 화면 테두리에 붉은 inset이 생긴다.
- 폭 축소 시: ≥1400 3열 / 1100~ 좌측 접기 / 900~ 좌측 숨김 / <900 모바일 안내.
  **축소가 아니라 재배치**다 — 줄이면 주문 버튼까지 작아진다.
- 좌·우·하단 경계는 드래그로 조절되고 비율이 저장된다 (범위 밖 값은 clamp).

### 모바일 재배치 — 완료 (같은 `/terminal`)

`MobileShell.tsx` + `BottomSheet.tsx`. **PC를 줄인 것이 아니라 조각을 다시 놓았다.**
- 세로: 차트가 화면 대부분 + 하단 고정 `[주문][포지션][호가][AI·뉴스]` → 바텀시트
- 가로: 시트를 안 쓰고 2열 (차트 | 패널). 가로에서 시트를 올리면 남는 높이가 없다
- **차트는 시트 아래에 계속 살아 있다** — 검증: 4개 시트를 열고 닫아도
  iframe이 같은 DOM 노드 (`chartSameNode: true`, 닫은 뒤에도 true)
- 주문 시트는 **끌어서 안 닫힌다**(`lockDrag`) — 수량 입력 중 미끄러지면 처음부터다.
  손잡이도 끌 수 있는 시트에만 보인다 (못 끄는데 손잡이가 있으면 거짓말)
- Kill Switch는 시트 안에 숨기지 않고 상단에 `STOP`으로 상주
- 호가에서 가격을 누르면 주문 시트로 갈아 끼우고 지정가에 채운다

### 디자인 토큰 정리 (`theme.ts`)
기존 팔레트가 촌스러웠던 이유 둘을 고쳤다:
1. 배경이 파란 네이비(#060B14)라 초록/빨강 손익색과 싸웠다 → 거의 무채색 먹색(#0A0B0D)
2. 8~10px 글씨 → 최소 10px, 본문 11~12px. 밀도는 글자가 아니라 여백·구분선에서 얻는다
- 경계선을 실선 대신 흰색 알파(.07) — 선이 보이면 화면이 조각나 보인다
- 색은 손익·경고·강조 셋만. 숫자는 전부 tabular-nums 고정폭
- 주문/Kill Switch 최소 46px 유지, 호가 행은 촘촘하게 (반대 방향으로 설계)

### 스트림 공유 (`useBinanceStream` 재작성)
훅을 부르는 곳마다 소켓을 새로 열고 있었다. 터미널만 해도 상단 가격·호가·주문
셋이 같은 심볼을 봐서 `depth20@100ms`가 세 벌 들어왔다 — 모바일에서는 그게 그대로
배터리와 데이터다. 심볼별 허브를 모듈 수준에 두고 `useSyncExternalStore`로 나눠 쓴다.
마지막 구독자가 떠나도 10초는 유지한다 (시트 여닫을 때마다 재연결하면 더 비싸다).
**검증**: 심볼을 SOL로 바꾸고 주문 시트까지 연 뒤 새로 열린 소켓 = **1개**.

**아직 안 한 것**: 차트 위 TP·SL 드래그, 다중 모니터 팝아웃, 단축키·명령 팔레트,
워크스페이스 여러 벌 저장, 체결 내역 탭(거래소 조회 추가 필요)

## PC 터미널 UI (원본 요청 명세)

기존 모바일 화면은 `모바일 모드`로 남기고, 새 화면을 `TRAIGO Pro 터미널`로 분리한다.
PC를 먼저 만들고, 모바일은 같은 컴포넌트를 다른 동선으로 재배치한다 (축소가 아님).

1920×1080 기준 배치:
```
좌 18%(시장·AI·뉴스·일정) | 중앙 55%(차트) | 우 27%(호가·주문·레버리지)
하단 25~30%: 포지션 | 미체결 | 체결 | 손익 | 전략 | Kill Switch
상단: 종목·가격·계좌·연결상태만 얇게
```
- 좌측 AI·뉴스 갱신이 중앙 차트를 재렌더링하지 않게 상태 격리
- 차트 심볼과 주문 심볼을 하나의 전역 선택 상태로 동기화
- 패널 크기·배치·심볼·주문설정을 사용자별 저장, 새로고침 후 복원
- 좁아지면 4패널 → 3패널 → 모바일 자동 전환
- 실전/Testnet을 배경색으로 구분
- 주문 버튼과 Kill Switch는 크게 유지. 고밀도는 뉴스·로그·통계에만 적용
- 모바일: 하단 고정 [주문][포지션][호가][AI·뉴스] + 바텀시트, 가로모드는 2열

## 신뢰성 기반 (요청 확정, UI보다 우선순위 높음)

사용자가 꼽은 핵심 5가지:
1. [x] **거래소 ↔ 앱 실시간 상태 대조** — `lib/engine/stateReconcile.ts` (순수 함수, 테스트 13개)
   + `GET /api/reconcile/state`. 심각(포지션 유무·방향·마진모드·손절소실)은 신규 주문 차단,
   경고(수량·레버리지)와 정보(잔고)는 차단하지 않는다. 허용 오차 수량 0.5%·잔고 1%.
   주문 경로(daily-ladder)에서 주문 직전 대조 후 409 차단, 화면에도 같은 근거를 배너로 표시.
   조회는 `reconcileCheck.ts`로 공용화 — 화면 판정과 주문 차단 근거가 항상 같다.
2. [x] **주문 전체 생명주기 + 중복 방지** — ``lib/engine/orderLifecycle.ts`` (상태 기계, 테스트 13개)
   13개 상태 + 허용 전이표. 종료 상태는 되돌릴 수 없고 건너뛰는 전이는 거부한다.
   orderExecutor의 update()가 이 검증을 통과한 전이만 기록한다 (DB의 INTENT/RECONCILED는 매핑).
   워커는 UNKNOWN(응답 없음)일 때 재시도하지 않고 멈춘다 — 중복 체결 방지.
   **남은 것**: 화면에 생명주기 표시, UNKNOWN 주문의 자동 조회 확정
3. [x] **연결 끊김·UNKNOWN 복구** — ``lib/engine/unknownResolver.ts`` (순수 함수, 테스트 11개)
   - **확정 규칙**: 거래소 조회 실패 / 반영 대기(5초) 내 / 멱등키 없음 / 포지션 못 읽음 /
     주문 없는데 포지션 변함 / 모르는 상태 문자열 → 전부 **확정하지 않는다**.
     멱등키 + 5초 경과 + 포지션 불변, 이 셋이 모두 맞아야 FAILED로 확정한다.
   - `reconcilePendingOrders`가 이 규칙을 쓴다. **기존 코드는 "조회에 안 보이면 즉시 FAILED"였다** —
     그러면 재시도가 열리고 그게 중복 체결이 됐다.
   - 근거 컬럼 추가: `pos_qty_before`(전송 직전 포지션), `resolve_attempts`, `needs_attention`
     → **마이그레이션 018 적용 필요** (아래 사용자 작업 참조). 적용 전에는 새 컬럼을 빼고
     다시 쓰는 폴백이 돈다 — 상태 기록이 통째로 날아가지 않게.
   - `assertStateConsistent`가 **미확정 주문이 1건이라도 있으면 신규 주문을 차단**한다.
   - exit-monitor cron이 청산 판단 **전에** 복구를 먼저 돌린다. 조회만 하고 재전송은 안 한다.
   - 호가 스트림에 **무응답 감시** 추가: 8초 조용하면 `⚠ 멈춤 N초` 표시, 25초면 강제 재연결.
     연결은 열려 있는데 데이터만 안 오는 경우 onclose가 안 와서 기존 재연결이 안 돌았다
     (실제로 겪은 문제다 — ticker 스트림이 정확히 이랬다).
4. [x] **데이터 출처·지연시간 표시** — ``lib/engine/dataQuality.ts`` (순수 함수, 테스트 12개)
   6가지 성격을 **서로 다른 모양**으로 구분한다 (색만 다르면 빠르게 훑을 때 안 보인다):
   `● 실시간(WS)` `○ 주기수신(REST)·계산값·AI추정` `◌ 모의(점선)` `⚠ 멈춤·수신없음`
   - 나이 기준을 값마다 다르게 잡는다. 호가는 100ms 주기, 변동률은 5초 주기다.
     하나로 묶으면 둘 중 하나는 반드시 거짓말이 된다.
   - 모의·추정은 아무리 새것이어도 `tradeable=false`다. 나이만 보면 그 구분이 사라진다.
   - "수신 없음"과 "멈춤"을 다른 상태로 둔다.
   - 매매 화면 배선: 호가 · 현재가 · 24h변동률 · 호가잔량 · 상단 대표가격
   **이 작업 중 드러난 실제 버그** (같이 고침): `useLivePrices`가 `action=coin`을 불렀는데
   그 응답은 코인 하나짜리 객체라 `fetchPrices`가 배열로 못 읽고 항상 빈 목록을 돌려줬다.
   결과적으로 **화면의 모든 종목 가격이 ASSETS 기본값을 난수보행한 값**이었고,
   상단 MOCK 배지만이 유일한 단서였다. BTC 헤더 ₩94,096,181 vs 호가창 63,3xx USDT(≈₩88M)로
   같은 화면에 두 값이 따로 놀았다. `all_crypto`로 바꿔 해결 — 이제 LIVE, 두 값이 일치한다.
5. [x] **단계별 운영 모드** — ``lib/engine/operatingMode.ts`` (순수 함수, 테스트 20개)
   `UI_DEMO → PAPER → TESTNET → SHADOW_LIVE → LIVE_SMALL → LIVE_LIMITED`
   - **한 칸씩만** 오른다. 건너뛰면 그 칸에서 얻었어야 할 증거가 통째로 빈다.
   - **하향은 언제나 허용**. 안전 밸브에 조건을 달면 정작 급할 때 못 내려간다.
   - 승급 조건: 표본 수 + 서로 다른 날짜 수 + 미확정 주문 0 + 심각 불일치 0
     (하루에 여러 건 넣어 기간을 채울 수 없다)
   - **Shadow Live**: 실계좌 시세로 판단은 전부 하되 주문만 안 보내고 `live_orders`에
     `SH` 접두사 + `status=INTENT`로 기록한다. 기록 안 하면 그냥 꺼둔 것과 같다.
     `status`가 SENT로 안 가므로 복구 대조(3번)가 이 행을 건드리지 않는다.
   - `LIVE_SMALL`은 $100 상한 **강제** + 건마다 `confirm:true` 필요(없으면 409).
   - 모르는 값은 `UI_DEMO`로 떨어진다. 예전 `LIVE`는 `LIVE_SMALL`로 옮겼다.
   - `GET /api/autotrade/mode` — 현재 단계·증거·다음 단계 가능 여부
   **로컬에서 실제로 확인**: `LIVE`/`LIVE_LIMITED` → 403(실거래 잠금),
   `REAL_MONEY_NOW`(오타 흉내) → `UI_DEMO`로 떨어짐, `SHADOW_LIVE`/`TESTNET` → 정상 판단 수행.
   **미확인**: 주문 승인이 나야 도달하는 `RECORD` 기록 경로 (그날 전략이 진입을 안 함)

성능: 호가·차트는 Canvas/WebGL 검토, 수신 200ms 쓰로틀링, 긴 내역 가상 스크롤,
보이지 않는 패널 갱신 축소, 심볼 변경 시 이전 요청 취소

보안: API 키 등록 화면에 **Worker 고정 IP 화이트리스팅 안내**, Secret은 등록 후 재표시 금지,
권한 분리(보기전용/모의/Testnet/실전수동/자동/관리자)

기타: 단축키·명령 팔레트, 다중 모니터 팝아웃, 워크스페이스 저장, 주문 초안,
이벤트 타임라인, 보안 화면, 법적 고지

## 검증 명령

```bash
npm test          # 유닛 테스트 621개 통과 / 0 실패 (테스트 파일 33개)
npm run typecheck # 타입 에러 80건
npm run build     # 프로덕션 빌드
```

2026-07-30 실행 결과다. `npm test`는 `node_modules`가 있어야 돈다 —
없으면 tsc를 못 찾고 "먼저 npm install을 실행하세요"로 멈춘다.

타입 에러 80건은 `next.config.js`의 `ignoreBuildErrors: true`가 덮고 있어서
빌드가 통과한다. 통과한다는 것이 맞다는 뜻은 아니다.

## 시장 분리 · 전략 계층 (완료)

`/terminal` 안에서 현물 / USDⓈ-M / COIN-M을 전환한다. 주문 경로·지갑·전략이 전부 분리돼 있다.

| 계층 | 파일 | 핵심 규칙 |
|---|---|---|
| 시장 유형 | `lib/markets/marketType.ts` | 없는 것은 0이 아니라 **없다**. 모르는 값은 `null` |
| 통합 자산 | `lib/markets/wallets.ts` | 현물 잔고를 선물 증거금에 **더하지 않는다** |
| 원가 | `lib/markets/costBasis.ts` | 코인으로 낸 수수료만큼 보유가 준다 |
| COIN-M | `lib/markets/coinM.ts` | 수량 단위가 **계약**. 계약 크기를 추측하지 않는다 |
| 전략 장부 | `lib/strategies/ledger.ts` | 자기 귀속분까지만 청산. 차이는 **미귀속**으로 |
| 현물 전략 10종 | `lib/strategies/spotStrategies.ts` | 의도 타입에 레버리지 필드가 **없다** |
| 현물 주문 도구 | `lib/strategies/spotOrderPlan.ts` | `guardedBy`로 거래소/앱 보호를 가른다 |
| 감시 루프 | `api/watch/tick` + GitHub Actions 15분 | **확인 주기를 화면에 그대로 적는다** |
| 결합 전략 6종 | `lib/strategies/combined.ts` | 반쪽 실행 시 **지금 상태**를 말한다 |

주문 경로는 시장마다 하나다 — `spot/order` · `futures/order` · `coinm/order`.
확인 토큰도 각각 다르다(`SPOT_` / `LIVE_` / `COINM_`). 같으면 엔드포인트만 바꿔도 통과한다.

**결합 전략 실데이터 확인** (BTCUSDT 63,523 · 펀딩 0.0001% · RSI 42.2):
- 증거금 1,000인데 헤지에 31,761 필요 → **차단** ("일부만 걸리면 헤지된 줄 알고 더 위험해집니다")
- 이미 숏 0.6 → 추가 0.4만 (기존 반영)
- 선물 다리 소실 → `BROKEN` "전액 노출 상태입니다"
- 한쪽 조회 실패 → `UNKNOWN` "정상이라는 뜻이 아닙니다"

## 정보 계층 — 뉴스 · AI · 캘린더 (완료)

이 문서가 한동안 놓치고 있던 부분이다. 시장 분리 이후 커밋 14개가 여기에 있다.

### 뉴스 파이프라인

수집과 분석을 **나눠 둔다**. 수집은 뉴스 API를, 분석은 AI API를 쓴다. 요금 체계도
실패 방식도 다르다. 붙여 두면 AI가 429를 뱉을 때 수집까지 죽고, 다음 회차에 원문을
처음부터 다시 받는다. 원문은 사실이라 한 번만 받으면 된다.

| 계층 | 파일 | 핵심 규칙 |
|---|---|---|
| 응답 검증 | `lib/news/schema.ts` | 검증을 통과 못 한 AI 응답은 흘리지 않는다. `uncertain`은 별도 값 |
| 수집·중복제거 | `lib/news/collect.ts` | 같은 기사에 AI를 두 번 부르지 않는다 (`contentHash`) |
| 기사 1건 분석 | `lib/news/analyzeOne.ts` | fetch를 주입받는다 — 키 없이도 동작을 검증할 수 있게 |
| 분석 대상 선정 | `lib/news/enrichPlan.ts` | **실패한 기사를 하루 96번 다시 부르지 않는다** |
| 합의 판정 | `lib/news/consensus.ts` | **갈렸을 때 평균을 내지 않는다** |
| 저장 | `lib/news/store.ts` | 원문 URL·발행 시각은 원문에서 온다 |

합의에서 평균을 금지하는 이유: 상승 80%와 하락 80%를 평균하면 "80% 확신"이 된다.
실제로는 두 모델이 정반대를 말한 것이고, 그건 아무것도 모르는 상태다. 숫자가 나오니까
화면에서는 확신 있어 보이는데, 그게 이 기능에서 가장 위험한 실패다.

화면: `components/news/AiVerdict.tsx` · `ConsensusPanel.tsx` — 판단 보류를 지우지 않는다.
API: `api/news/stored` · `api/ai/news/analyze` · `api/ai/consensus`

### AI 비용·응답시간 관리센터

`lib/ai/pricing.ts` + `components/pages/AiUsagePage.tsx` + `api/ai/usage`

**모르는 모델을 0원으로 적지 않는다.** 단가표에 없는 모델을 0으로 두면 화면에
"오늘 $0.00"이 뜬다. 사용자는 그걸 '무료'로 읽지 '모른다'로 읽지 않는다. 실제로는 돈이
나가고 있는데 화면만 안전해 보이는 상태다. 결과에 '추정'과 '단가 기준일'을 같이 들고 나간다.

관리자 전용이다 (`f98becc`) — **메뉴를 숨기는 것은 잠금이 아니다.**
OpenAI 오류는 사유를 실어 나른다 (`a73fc15`) — 429만으로는 대응을 정할 수 없다.

### 경제 캘린더

`lib/calendar/normalize.ts` + `api/cron/calendar-sync`

- **시각을 확정 못 하면 저장하지 않는다.** 시간대를 잘못 읽으면 FOMC가 몇 시간 밀리고,
  화면에는 그냥 '오후 2시'로 보인다. 사용자는 그 시각에 맞춰 포지션을 정리한다
- **발표 전 예상치를 실제치 자리에 넣지 않는다** (`677ba79`에서 고침). 아직 안 나온
  숫자를 "CPI 3.2% 나왔다"로 적는 것은 뉴스가 아니라 거짓말이다. 화면이 출처를 말한다

### 데이터 정리 (retention)

`lib/maintenance/retention.ts` + `api/cron/cleanup`

삭제는 되돌릴 수 없는데 실수는 조용하다. `?days=0`을 한 번 잘못 넘기면 컷오프가 '지금'이
되어 테이블 전체가 지워지고, 응답에는 "정리 완료 12,483건"이라고 뜬다. 성공처럼
보인다는 게 제일 나쁘다. 세 겹으로 막는다 — 보관 기간 하한 · 삭제 개수 상한 ·
표 전체를 지우려 하면 멈춤. **세지 못했으면 지우지 않는다.**

### 크론 배치

Vercel Hobby는 Cron이 하루 1회뿐이고 그 자리는 `exit-monitor`가 쓴다.
하루에 한 번 모으는 뉴스는 뉴스가 아니라서 나머지는 GitHub Actions로 옮겼다.

| 워크플로 | 주기 | 하는 일 |
|---|---|---|
| `vercel.json` cron | 하루 1회 00:00 UTC | `exit-monitor` (청산 감시) |
| `news-pipeline.yml` | 30분 | 뉴스 수집 → 캘린더 → AI 분석 → (UTC 18시에만) 정리 |
| `watch-tick.yml` | 15분 | 트레일링·예약 감시 |
| `exit-monitor.yml` | — | 청산 감시 백업 경로 |

뉴스 파이프라인은 **503(키 없음)만 실패로 표시한다.** 기사 하나가 이상해서 분석이 안
되는 것은 흔한 일이고, 그때마다 빨간 표시가 뜨면 진짜 고장을 놓친다.

### 터미널 · 내비 · 테마

| 작업 | 파일 | 핵심 |
|---|---|---|
| 선물 주문판 | 바이낸스 배치로 재구성 | **방향 토글을 없앤다** (매수/매도 버튼이 곧 방향) |
| 포지션 카드 | `lib/markets/positionView.ts` | 9열 표를 버리고 **0과 '없음'을 구분한다** |
| 시장가 청산 | 포지션 카드에서 직접 | 방향을 테스트로 못 박는다 |
| 오버레이 순서 | `lib/nav/overlayStack.ts` | 뒤로가기는 **맨 위 하나만** 닫는다 |
| 주소 동기화 | `app/page.tsx` | 주소창과 화면을 앱 전체에서 일치 |
| 테마 | `lib/theme/themeMode.ts` · `colors.ts` | 앱과 터미널이 한 팔레트. 밝음/어두움/시간 |

`positionView.ts`를 컴포넌트 밖으로 뺀 이유: 틀리면 안 되는 계산이 셋 있다 —
청산가가 0일 때, 증거금을 거래소가 안 줄 때, 수익률의 분모가 0일 때.
셋 다 화면에서는 그럴듯한 숫자로 보여서 눈으로는 틀린 걸 알 수 없다.

### 이 시기에 같이 고친 실제 버그

- `d6052a9` 가격 없는 종목이 **₩0으로** 뜨던 것 — 0원은 시세가 아니라 고장이다
- `1edf566` **'live'라고 적으면서 지어낸 수치를 섞던 것** — 시간대·출처 점검
- `5b28524` 더보기 메뉴 회귀 (위 '미처리 UI' 참조)

## 빠른 명령 팔레트 (Ctrl/Cmd+K) — 1차 완료

한글 **초성 검색** 포함. 테스트 54개.

### 왜 이것부터인가

팔레트와 전역 단축키는 아키텍처 레벨에서 같은 기능이다 — 둘 다 "실행 가능한 행동의
목록"이 필요하다. 레지스트리를 먼저 만들면 팔레트는 그 위의 **뷰**, 단축키는 **키맵**이
된다. 단축키를 먼저 만들면 `Shift+B → 매수`를 핸들러에 박게 되고 나중에 다 뜯는다.

그리고 `MENU`(lib/menuItems.tsx)가 이미 레지스트리의 80%였다 — 31개 항목에 검색
키워드(`kw`)까지 들어 있었다. 없던 것은 실행 방법과 위험 등급 둘뿐이다.

| 파일 | 역할 |
|---|---|
| `lib/commands/types.ts` | 커맨드 모양 + **위험 등급**. import가 없다 |
| `lib/commands/hangul.ts` | 초성 분해·비교 (순수) |
| `lib/commands/match.ts` | 점수·순위 (순수) |
| `lib/commands/registry.ts` | MENU → 커맨드 변환 + 추가 커맨드 (순수) |
| `lib/commands/useCommandHotkey.ts` | Ctrl/Cmd+K 리스너만 |
| `components/CommandPalette.tsx` | 뷰. 판단을 다시 하지 않는다 |

### 위험 등급을 창구가 아니라 커맨드에 둔다

```
safe      화면 이동·검색·테마 → 즉시
guarded   위험을 늘리는 것(주문)  → 확인 한 단계 (Enter 두 번)
reducing  위험을 줄이는 것(킬스위치) → 즉시
```

`reducing`을 따로 둔 이유: "위험한 건 다 확인"으로 묶으면 정작 급할 때 킬스위치 앞에
확인창이 뜬다. 그 0.5초가 이 앱에서 가장 비싼 0.5초다. 반대로 매수를 즉시 실행하면
오타 한 번이 포지션이 된다. **방향이 다르면 처리도 달라야 한다.**

등급을 키 핸들러가 아니라 커맨드에 두는 이유: 창구가 앞으로 여럿(팔레트·단축키·
플로팅 버튼)이 되는데, 창구마다 판단하면 그중 하나는 반드시 빼먹는다.

**지금 실려 있는 커맨드는 전부 `safe`다.** 킬스위치를 팔레트에서 바로 발동시키지
않았다 — `/api/risk/kill-switch/trigger`가 `connectionId`를 요구하고, 어느 연결을
끊을지 팔레트는 모른다. 그런데도 `reducing`을 달면 배지는 "즉시 실행"인데 실제로는
화면만 열린다. 급할 때 그 배지를 믿고 Enter를 친 사람은 자동매매가 멈춘 줄 알고 손을
뗀다. 테스트가 "실린 커맨드는 전부 safe"를 못 박아서, 나중에 위험 동작을 배선할 때
**먼저 실패해서 확인 흐름도 같이 붙이라고 알려준다.**

### 초성 매칭에서 정한 것

- **호환 자모**(U+3131 `ㄱ`)를 쓴다. 음절을 분해해 얻는 초성(U+1100 `ᄀ`)과 코드가
  다른데 눈으로는 같아 보인다. 섞으면 "왜 검색이 안 되지"가 된다
- 호환 자모 블록에 `ㄳ`·`ㄵ` 같은 겹받침이 끼어 있어 **코드 산술로 초성 번호를 구할 수
  없다.** 초성 19자를 순서대로 적어 인덱스를 쓴다 (쌍자음 테스트로 못 박음)
- **건너뛰기 없음.** `ㄱㅋ`는 경제캘린더에 안 맞는다 (`ㄱㅈㅋ`가 맞다). 허용하면
  `ㅁㅁ`이 거의 전부에 맞아 결과가 목록 전체가 된다 — 그건 검색이 아니다
- `ㄱ`은 `ㄲ`에 맞지 않는다. 느슨하게 하면 한 글자 질의 결과가 두 배가 되고 순위가
  무의미해진다
- **모음만 치면 결과 0건**이고 던지지 않는다
- 초성으로 맞은 것은 계층을 **낮춰** 기록한다. 그러지 않으면 `ㅈㄷ`와 `자동`이 같은
  점수를 받아, 정확히 친 사람이 초성만 친 사람과 같은 대우를 받는다
- kw·한글 이름에도 초성이 통한다 — 라벨에만 통하면 한글 사용자에게 kw는 무용지물이다

순위는 사용 빈도를 학습하지 않는다. 같은 입력이 어제와 다른 결과를 내면 손이 기억한
순서가 매번 틀린다. 첫 줄이 중요한 이유는 사용자가 `Ctrl+K → 타이핑 → Enter`를
습관으로 만들기 때문이다.

### 실제 MENU 29개 + 종목으로 확인

| 질의 | 첫 결과 |
|---|---|
| `ㄱㅈㅋ` | 경제캘린더 |
| `ㅂㅌㅅㅌ` | 백테스트 |
| `ㅍㅌㅍㄹㅇ` | 포트폴리오 |
| `ㅁㅁ` | 매매하기 (모의매매·자동매매보다 앞에서 맞았다) |
| `ㅋㅅ` | 리스크 관리 — 라벨에 '킬스위치'가 없는데 kw에서 잡혔다 |
| `fomc` | 경제캘린더 (kw) |
| `ㅅㅅㅈㅈ` | 005930 (삼성전자) |
| `ㅇㅂㄷㅇ` | NVDA (엔비디아) |

**이 확인 중에 내가 만든 버그를 찾았다**: `ㅋㅅ` 결과에 '리스크 관리'가 두 줄로 나왔다.
MENU에 이미 있는 항목을 EXTRA_COMMANDS에 또 적었기 때문이다 — 이 파일 맨 위에 적어 둔
"메뉴를 다시 적지 않는다"를 내가 어긴 것이었다. 픽스처 테스트만으로는 안 보였고,
실제 MENU로 돌려봐야 보였다.

### 남은 것

- 전역 단축키 (레지스트리 위의 키맵). `Shift+B` 같은 주문 단축키는 `guarded` 배선이 먼저
- 종목 결과가 `useLivePrices`의 목록에 한정된다. 거래소 전체 300개+는 `SymbolSearch`가
  따로 갖고 있어 아직 합치지 않았다
- 최근 방문·즐겨찾기를 팔레트 상단에 (즐겨찾기 자체는 이미 있다)
- 색맹 모드 · 패널 팝아웃 (팝아웃은 차트 iframe 동일 노드 보장과 충돌한다 — 주의)

## 관리자 화면 차단 (완료)

미들웨어가 `/admin`·`/developer`를 **서버에서** 막는다. 테스트 39개.

### 무엇이 문제였나

`middleware.ts`는 아무것도 막지 않았고, 파일에 그 이유가 주석으로 적혀 있었다 —
세션이 localStorage에 있어 미들웨어가 볼 수 없다는 것. 사실이었지만 두 가지가 남았다.

1. **`/developer`의 문지기가 위조 가능했다.** `getMockSession()`으로 localStorage의
   `tg_mock_session_v2`를 읽어 role을 봤다. 콘솔에서 그 JSON의 role만 바꾸면 화면이
   열렸다 — 문지기가 방문자 주머니에 들어 있던 셈이다
2. 화면은 열리고 데이터만 403이라 **"잠긴 줄 알았는데 안 잠긴"** 상태였다

### 어떻게 고쳤나

세션 전체를 쿠키로 옮기지 않았다. 그러면 저장소 어댑터·refresh token·4KB 조각내기까지
로그인 흐름 전체를 건드린다. 대신 **access token만 쿠키(`tg_at`)로 복사**했다.
원본은 그대로 localStorage에 있고 로그인·로그아웃 코드는 손대지 않았다.

| 파일 | 역할 |
|---|---|
| `lib/auth/adminGate.ts` | 통과 판정 (순수 함수 + 조회). 테스트가 붙는 자리 |
| `middleware.ts` | 쿠키를 꺼내 판정에 넘기고 리다이렉트만 한다 |
| `components/auth/SessionCookieSync.tsx` | 세션 토큰 → 쿠키 복사. 레이아웃에 상주 |
| `lib/auth/clientRole.ts` | 화면이 '내 role'을 묻는 하나의 경로 |

핵심 규칙:
- **role을 토큰에서 읽지 않는다.** JWT payload는 서명 없이 누구나 만든다 —
  `{"role":"super_admin"}`은 base64 한 번이다. 디코드해 쓰는 값은 `sub`·`exp` 둘뿐이고
  권한은 Supabase가 서명을 검증한 뒤 돌려준 role로만 정한다
- **PostgREST 한 번으로 서명 검증과 권한 확인을 같이 한다.** 서명이 틀리면 401이고,
  RLS(`profiles_self_read`)가 걸려 있어 남의 행은 애초에 안 읽힌다. service role 키를
  미들웨어에 들고 다니지 않는다
- **조회에 실패하면 통과시키지 않는다.** 정전에 풀리는 잠금장치는 잠금장치가 아니다.
  대신 관리자가 잠깐 못 들어오는 쪽을 택했다 (화면 2개고 API는 따로 막혀 있다)
- **모르는 role 문자열은 막는다.** '일단 통과'를 택하면 역할 하나 추가할 때 문이 열린다
- `/developer`는 `/admin`보다 등급이 높다 — admin 계정은 /developer에 못 들어간다.
  기존 `canAccessDeveloper()`와 같은 기준이다. 두 곳이 다르면 화면과 미들웨어가
  서로 다른 말을 한다

같이 고친 것:
- `/admin`이 `role !== 'admin'`으로 판정해 **developer·super_admin을 막고 있었다.**
  등급이 더 높은데 그 화면만 다른 말을 했다 → `canAccessAdmin()` 하나로 통일
- `/admin`과 `/developer`가 같은 질문에 각자 다른 방법으로 답하고 있었다 →
  `clientRole.ts` 하나로 모음
- `/unauthorized`가 거절 사유를 표시한다. `lookup_failed`는 권한 문제가 아니라
  서버가 확인을 못 한 것이라 '다시 시도'가 답인데, "권한 없음"만 보이면 계정을 의심한다

### 실제로 확인한 것

프로덕션 빌드를 띄우고 curl로 (전부 307 리다이렉트):

| 요청 | 결과 |
|---|---|
| 쿠키 없이 `/admin` | `/auth?next=%2Fadmin&reason=no_token` |
| 쿠키 없이 `/developer` | `/auth?next=%2Fdeveloper&reason=no_token` |
| `/admin/anything` (하위 경로) | `/auth?next=%2Fadmin%2Fanything&reason=no_token` |
| 쓰레기 쿠키 | `reason=bad_token` |
| 만료 토큰 | `reason=expired` (네트워크 안 부름) |
| **payload에 `role:super_admin`을 심은 위조 토큰** | `reason=unverified` — 거부됨 |
| `/terminal` (보호 대상 아님) | 200, 그대로 통과 |

**아직 확인 못 한 것**: 실제 관리자 계정으로 통과하는 경로. 이 컨테이너에 진짜 anon
키가 없어 유닛 테스트(가짜 PostgREST)로만 검증했다. 배포 후 관리자로 `/admin`에
들어가 보는 것이 남았다 — 만약 막히면 `/unauthorized`의 **사유 코드**를 보면 된다
(`lookup_failed`면 조회 문제, `insufficient_role`이면 DB의 role 문제다).

**한 번 튕길 수 있다**: 배포 직후 첫 방문은 쿠키가 아직 없어 `/auth`로 갔다가
되돌아온다. `/auth`가 `next` 파라미터로 원래 목적지에 다시 보내고, 그때는 쿠키가
생겨 있어 통과한다. `next`는 같은 사이트 경로만 따라간다 — 로그인 화면이
오픈 리다이렉트가 되면 피싱에 그대로 쓰인다.

**이 장치의 한계**: 미들웨어는 페이지 요청만 막는다. JS 번들 파일 자체는 여전히
공개 URL이다. 코드를 숨기는 장치가 아니라 화면에 들어가지 못하게 하는 장치이고,
데이터의 방어선은 계속 API의 `requireAdmin()`이다.
