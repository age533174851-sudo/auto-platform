# TRAIGO 작업 현황

> 대화가 길어져 컨텍스트가 요약될 수 있으므로 남기는 인수인계 문서.
> 마지막 갱신: 2026-07-30
>
> **이 문서를 고칠 때**: 숫자(테스트 수·타입 에러·마이그레이션 번호)는 기억이 아니라
> 실행 결과를 적는다. 낡은 숫자는 없는 것보다 나쁘다 — 이 문서가 한 번 그랬다.
> 테스트 수가 55로 적혀 있는 동안 실제로는 528개였고, 019·020 마이그레이션은
> 적용 필요 목록에 아예 없었다.

## 지금 상태 한눈에

**돌아가는 것**: 신뢰성 기반 5가지 · PC/모바일 터미널 · 빠른 명령 팔레트(Ctrl+K, 초성 검색) · 전역 단축키 · 거래 전 점검 ·
시장 분리(현물·USDⓈ-M·COIN-M) ·
현물 전략 10종 + 결합 전략 6종 · 뉴스·AI·캘린더 정보 계층 · 실시간 호가창

**막혀 있는 것**: 마이그레이션 3건(018·019·020) + 시크릿 미설정.
019가 없으면 뉴스가 저장되지 않고, `ADMIN_SECRET`이 없으면 청산 감시가 401이다.
**테스트넷 가동(다음 단계 A)이 여기서 멈춰 있다.**

**제일 위험한 것**: Gate 손절 부착(`price_orders`)이 **실계좌로 검증되지 않았다.**
문서만 보고 만든 요청이다. 그래서 손절이 실패하면 방금 연 포지션을 즉시 닫도록
해뒀다(아래 "Gate 거래소 경로" 참조) — 최악의 결과가 '보호 없는 포지션'이 아니라
'포지션 없음 + 실패 보고'가 되게. **테스트넷에서 한 번은 직접 확인해야 한다.**
COIN-M 손절(`STOP_MARKET` + `closePosition`)도 같은 상태다 — 요청 모양만 확인했다.

| 검증 | 값 (2026-07-30 실행) |
|---|---|
| 유닛 테스트 | 897 통과 / 0 실패 |
| 타입 에러 | 80건 (`ignoreBuildErrors`로 덮여 있음) |
| 프로덕션 빌드 | 통과 · 미들웨어 27.6 kB |

## 인프라

| 항목 | 상태 |
|---|---|
| Vercel | `auto-platform-zeta.vercel.app` · regions `hnd1`(도쿄) · Hobby 플랜 |
| GitHub | `age533174851-sudo/auto-platform` · main 브랜치 |
| Supabase | ref `sgbysrvvxlluzffmgcho` · 39개 테이블 |
| Railway 워커 | **사용 안 함** — Binance가 IP 지역 차단 (`Service unavailable from a restricted location`) |

**Binance 도달성**: Vercel(hnd1)에서는 선물·현물·테스트넷 모두 정상. Railway만 차단됨.
그래서 청산 감시를 Vercel Cron + GitHub Actions로 옮겼다.

## 아직 안 된 설정 (사용자 작업)

코드로는 뚫을 수 없는 것들이다. **이 중 마이그레이션과 시크릿이 안 되면 다음 단계 A
(테스트넷 가동)는 시작 자체를 못 한다.**

### 마이그레이션 3건 미적용

Supabase SQL 편집기에 파일 내용을 붙여넣고 실행. (관리 API 토큰이 환경에 없어
자동 적용을 못 했다.)

- [ ] `018_live_orders_recovery.sql` — 적용 전에도 앱은 돌지만 UNKNOWN 주문의
      포지션 교차 확인이 생략된다. 새 컬럼을 빼고 다시 쓰는 폴백이 돈다
- [ ] `019_news_articles.sql` — **없으면 뉴스 파이프라인이 아무것도 저장하지 못한다**
- [ ] `020_ai_usage_detail.sql` — 없으면 AI 비용·응답시간 관리센터가 빈 표로 뜬다

### 시크릿·환경변수

- [ ] `ADMIN_SECRET` — Vercel 환경변수 + GitHub Secret(`EXIT_MONITOR_SECRET`)에 동일 값
      없으면 청산 감시 cron이 401로 실패한다
- [ ] GitHub Secret `EXIT_MONITOR_URL` = `https://auto-platform-zeta.vercel.app`
      (뉴스 파이프라인 워크플로도 이 값을 쓴다)
- [ ] GitHub Secret `NEWS_CRON_SECRET` = Vercel의 `CRON_SECRET`과 같은 값
      없으면 뉴스 수집·캘린더·AI 분석·정리 4단계가 전부 401
- [ ] 뉴스 공급자 키(`FINNHUB_API_KEY` 등) + AI 키(`OPENAI_API_KEY` 등)를 Vercel에
      없으면 각 단계가 **503으로 그 사실을 알려준다** (조용히 빈 값을 넣지 않는다)

### 외부 발급이 필요한 것

- [ ] 바이낸스 테스트넷 연결 재등록 (암호화 키가 바뀌어 기존 항목은 못 읽음)
- [ ] Google OAuth 클라이언트 ID/비밀 — 로그인 실패의 원인. 아래 '미처리 UI' 참조
- [ ] 대화 초반 노출된 Supabase/Vercel 토큰 폐기

## 실데이터 검증 결과 (핵심)

09:00 KST 전환 → N분 관찰 → 1회 진입 → 다음 09:00 청산 전략.
Mark Price 1분봉 기준, 비용(수수료 0.08% + 슬리피지 0.03% + 펀딩 0.01%×3) 반영.

| 심볼 | 기간 | MAE 중앙값 | 100배 생존률 | 100배 기대값 |
|---|---|---|---|---|
| BTCUSDT | 90일 | 1.29~1.49% | 18~21% | -77~-82% |
| BTCUSDT | 365일 | 1.28~1.36% | 17~21% | -78~-80% |
| ETHUSDT | 365일 | 1.75~1.98% | 14~18% | -80~-85% |

**결론: 구조적이다.** 기간을 4배로 늘리고 심볼을 바꿔도 동일.
MAE 중앙값이 100배 청산거리(0.5%)의 **2.6~4배**다.
방향을 맞혀도 23시간 30분 보유 중 중간 역행에서 청산된다.

**단, 방향 적중률(41~52%)은 사용자 실력이 아니라 검증 스크립트의 임의 판단 규칙 성적이다.**
(몸통 30% 이상 + 반대꼬리가 몸통보다 짧으면 진입)
MAE 분포는 진입 규칙과 무관하므로 그 결론만 유효하다.

재현: `npm run validate:real` (일반) / `node scripts/validate-09kst.mjs BTCUSDT 365` (09시 전략)

## 다음 단계 (사용자 합의 순서)

- [x] **D** 교차 확인 — BTC 365 + ETH 365 완료
- [ ] **A** 테스트넷 가동 — **막혀 있다.** `ADMIN_SECRET` + 마이그레이션 018이 먼저다
      (위 '아직 안 된 설정'). 시작되면 하루 1표본씩 누적
- [x] **B** 비상 종료 — `positionGuard.ts` 작성 + `/api/autotrade/exit-monitor`에 배선 완료
- [ ] **C** 판단 규칙 개선 후 재측정 — **사용자의 실제 진입 기준을 받아야 시작할 수 있다.**
      현재 측정에 쓴 규칙(몸통 30% + 반대꼬리)은 내가 임의로 정한 것이다

A와 C 둘 다 코드가 아니라 입력을 기다리는 상태다. A는 시크릿, C는 진입 기준.



## 진행 중 / 확인 필요

### ✅ 실시간 호가창 — 완료 (프로덕션 확인됨)
`Math.random()` 가짜 호가를 Binance 실시간 데이터로 교체했다.
프로덕션(auto-platform-zeta.vercel.app)에서 확인:
```
가격 (USDT) | ● 실시간 | 수량
63,314 | 0.002  …  63,316 USDT ▼2.66%  …  63,313 | 17.599
호가 잔량 매수 95.8% | 4.2% 매도
```

거쳐온 문제 3가지 (재발 시 참고):
1. **심볼 슬래시** — `sel.sym`이 'BTC/USDT'라 `.replace(/USDT$/,'')+'USDT'`가
   'BTC/USDT'를 만들어 없는 스트림에 붙었다. Binance는 연결만 받아주고 데이터를
   안 보내서 "● 실시간인데 비어 있음"이 됐다. 영숫자만 남기도록 수정.
2. **단위 혼입** — 현재가가 stream.lastPrice 없을 때 원화(sel.p)로 폴백해
   호가는 63,654(USDT)인데 중앙만 94,061,179(KRW)가 됐다. 최우선 호가로 채우고
   'USDT' 명시.
3. **스트림 지역 제한** — Node로 측정한 결과 (브라우저 무관하게 재현):
   depth20 정상 / bookTicker 1737건 / **aggTrade·ticker·markPrice·kline 모두 0건**.
   현물 aggTrade는 80건 옴. 코드 버그가 아니라 접속 지역 제한이다.
   → 현재가는 bookTicker 중간값, 변동률은 REST /fapi/v1/ticker/24hr 5초 폴링,
     매수/매도 강도는 체결 대신 **호가 잔량**(bookImbalance)으로 대체.
     화면 표기도 '호가 잔량 매수'로 바꿔 체결 강도와 구분했다.
   ※ 현물 aggTrade는 오지만 쓰지 않았다 — 선물 호가에 현물 체결을 섞으면
     서로 다른 시장 값이 한 화면에 놓이고 사용자가 구분할 방법이 없다.
   ※ WebSocket은 사용자 브라우저에서 직접 Binance로 붙으므로, 이 제한은
     서버 리전이 아니라 접속자 네트워크 기준이다. 지역이 다르면 ticker가
     정상적으로 올 수도 있다 — 그 경우에도 현재 구조는 문제없다(오는 것만 씀).

확인 절차: 매매 → 상단 서브탭 '매매'(y≈206, 하단 네비 아님) → 코인 선택
### 사용자가 지적한 미처리 UI
- ~~하단 `더보기` 메뉴~~ ✅ 해결 (`5b28524`). 원인은 내가 넣은 모달 뒤로가기였다:
  주소 동기화가 `pushState`로 탭을 쌓고, 모달 정리가 그걸 `history.back()`으로
  되감아 눌러도 제자리였다. 두 곳에 같은 기준(맨 위가 모달 표식인가)을 넣어 고쳤다.
  브라우저에서 백테스트·뉴스·포트폴리오 3항목 이동 확인.
- Google 로그인 실패 — Supabase `external_google_enabled = False`.
  **활성화하려면 Google Cloud OAuth 클라이언트 ID/비밀번호가 필요하다 (사용자 발급 필수).**
  리디렉션 URI: `https://sgbysrvvxlluzffmgcho.supabase.co/auth/v1/callback`
- 로컬 개발용 `.env.local`은 생성 완료 (Supabase URL/anon/service 키)

### 요청받은 화면 스펙 (스크린샷 제공됨)
Binance Futures 스타일 — PC 3열(차트 | 호가+체결 | 주문폼), 모바일 세로 스택.
사용자가 스크린샷 3장을 제공했다. 반영 시 그 구조를 따를 것.

## 매매 화면 개편 (일부 완료)

사용자가 요청한 우선순위:
1. ~~거래소-앱 포지션 자동 대조~~ ✅ **완료** — 신뢰성 1번(`stateReconcile.ts`)이 이걸
   덮었다. `GET /api/reconcile/state` + 주문 직전 409 차단 + 화면 배너
2. 계좌 위험판 — **미착수.** `calcLiquidationPrice`, ISOLATED 검사, 킬스위치 재사용
3. 포지션/미체결 통합 관리 — **일부.** 포지션 카드와 시장가 청산은 됐고
   (`positionView.ts`), 미체결 통합은 남았다
4. ~~실시간 호가·체결~~ ✅ 완료 (위 참조)
5. 차트 위 TP·SL 드래그 — **미착수.** 코드에 흔적 없음을 확인했다

별도로 09시 전략 전용 화면 7단계 명세도 받아둠 (관찰 패널 · LONG/SHORT 판정판 ·
100배 생존 패널 · 주문 확인 · 포지션 보유 · 09시 교체).

## 감사 지적 처리 현황

| # | 내용 | 상태 |
|---|---|---|
| 1 | 웹훅 connectionId 위조 | ✅ user_id 소유권 검사 추가 (근본 해결은 사용자별 토큰) |
| 2 | ISOLATED 미강제 | ✅ `/fapi/v1/marginType` 호출 추가, 실패 시 주문 중단 |
| 3 | 손절 실패인데 진입 성공 | ✅ 즉시 청산 + ok:false |
| 4 | 워커 lock 중복 주문 | ✅ 락 no-op 제거 · stale 회수 조건 강화 · clientOrderId 멱등 |
| 5 | Expansion 미적용 | ✅ 주문 경로 연결 + 상한 5→100 개방 |
| 6 | TP/SL 수정이 타 주문 취소 | ✅ closePosition=true인 STOP/TP만 선별 취소 |
| 7 | DB 컬럼명 불일치 | ✅ `api_key_enc`(없는 컬럼) → `api_key`/`api_secret_enc` |
| 8 | reconcile 무인증 | ✅ 관리자 JWT 또는 x-admin-secret 요구 |
| 9 | 캘린더 아무나 변조 | ✅ `checkAdminRole`로 관리자 전용 |
| 10 | 전략 중복(10슬롯 vs 하루1회) | ❌ 미처리. `slotManager.ts`는 폐기 표시만 되어 있음 |

추가 발견: `/api/safety`에 하드코딩 관리자 시크릿(`traigo-admin-dev`) → 제거 완료

## 미처리 과제

위험한 순서로 적는다.

1. **Gate·COIN-M 손절 부착을 테스트넷에서 실제로 확인** — 코드는 들어갔지만
   (아래 두 절) Gate `price_orders`와 COIN-M `STOP_MARKET`+`closePosition` 호출은
   실계좌 응답을 본 적이 없다. 확인한 것은 나가는 HTTP의 모양이다.
   되돌리기가 있어 안전 쪽으로 실패하지만, '항상 되돌려지는' 상태는 정상이 아니다.
2. **AI 100X 엔진 나머지**: 시장 상태 필터 · 트레일링 스탑 · 연승/연패 크기
   조절. 셋 다 **백테스트 쪽에는 이미 있고** 실거래 엔진에 연결만 안 되어 있다.
3. **킬스위치가 COIN-M을 건드리지 않는다** — `executeKillActions`는 fapi만 부른다.
   COIN-M 포지션이 있으면 킬스위치를 눌러도 그 포지션은 그대로 남는다.
4. **타입 에러 80건** — `next.config.js`가 `ignoreBuildErrors: true`로 덮고 있다
5. `slotManager.ts` 정리 (감사 10번). 폐기 표시만 되어 있고 파일은 남아 있다:
   `src/lib/strategies/slotManager.ts`
6. 신뢰성 2번 잔여: 주문 생명주기를 **화면에 표시**, UNKNOWN 주문의 자동 조회 확정

~~`middleware.ts`가 `/admin`·`/developer`를 막지 않는다~~ ✅ 해결 (아래 절 참조)

## 추가 요청 기능 (사용자 제시, 미착수)

플랫폼 완성도 항목 — 사용자가 꼽은 상위 5개를 먼저:
1. **매매 리플레이** — 과거 날짜 선택 → 09시 전환·관찰·가상 진입·24시간 결과 재생
2. **전략별 독립 장부 + 충돌 관리** — 장부는 `lib/strategies/ledger.ts`로 **완료**
   (자기 귀속분까지만 청산, 차이는 '미귀속'으로). **남은 것은 충돌 시 정책 선택 UI**
3. ~~**거래 전 자동 체크리스트**~~ ✅ **완료** — 아래 '거래 전 점검' 절 참조
4. **계좌 스냅샷·장애 복구** — 09시와 주문 전후 자동 저장, 재시작 시 거래소와 대조 복구
5. **수익 보호 금고** — 보호 수익을 증거금 계산에서 제외, 목표 도달 시 새 사이클 자동 생성

나머지: ~~실시간 데이터 품질 표시~~ · ~~기능 신뢰도 표시(실시간/지연/모의/AI추정 구분)~~
→ 둘 다 신뢰성 4번(`dataQuality.ts`)으로 **완료**. 나머지는 미착수:
주문 실행 품질 분석, 사고 대응 화면, 성과 원인 분석(요일·관찰시간·첫봉형태별),
매매 비교 실험실, 고급 알림 규칙, 워크스페이스 저장, 내역 내보내기(CSV/PDF).

신뢰도 표시가 특히 중요했던 이유 — 호가창이 가짜 데이터를 보여주던 것이 실제 사례였고,
그 뒤 `useLivePrices`가 **모든 종목 가격을 난수보행 값으로 공급하던 것**도 같은 종류였다.
둘 다 화면에서는 정상으로 보였다.

## PC 터미널 UI — 1차 완료 (`/terminal`)

`components/terminal/` — Shell · Context · TopBar · LeftRail · ChartPane · OrderPane · BottomDock

**실측 확인** (1600×1080 기준, 브라우저에서 직접 측정):
| 항목 | 스펙 | 실측 |
|---|---|---|
| 좌 | 18% | 17.9% (287px) |
| 중앙 | 55% | 54.7% (876px) |
| 우 | 27% | 26.9% (430px) |
| 하단 | 25~30% | 26.4% (238px) |
| 상단 | 얇게 | 46px |

- **상태 격리 검증됨**: 좌측 탭을 시장→뉴스→일정→AI로 4번 돌려도
  차트 iframe이 **같은 DOM 노드**로 유지된다 (sameNode: true, src 불변).
  뉴스가 갱신될 때마다 차트가 리셋되면 못 쓰는 화면이 된다.
- **심볼 동기화 검증됨**: 상단에서 ETH로 바꾸면 차트·주문버튼·수량 placeholder가
  한꺼번에 따라오고 localStorage에 저장된다.
- 주문 버튼 415×46, Kill Switch 106×44 — 크게 유지. 고밀도는 뉴스·시장 목록에만.
- 실자금 모드면 상단 띠가 붉어지고 화면 테두리에 붉은 inset이 생긴다.
- 폭 축소 시: ≥1400 3열 / 1100~ 좌측 접기 / 900~ 좌측 숨김 / <900 모바일 안내.
  **축소가 아니라 재배치**다 — 줄이면 주문 버튼까지 작아진다.
- 좌·우·하단 경계는 드래그로 조절되고 비율이 저장된다 (범위 밖 값은 clamp).

### 모바일 재배치 — 완료 (같은 `/terminal`)

`MobileShell.tsx` + `BottomSheet.tsx`. **PC를 줄인 것이 아니라 조각을 다시 놓았다.**
- 세로: 차트가 화면 대부분 + 하단 고정 `[주문][포지션][호가][AI·뉴스]` → 바텀시트
- 가로: 시트를 안 쓰고 2열 (차트 | 패널). 가로에서 시트를 올리면 남는 높이가 없다
- **차트는 시트 아래에 계속 살아 있다** — 검증: 4개 시트를 열고 닫아도
  iframe이 같은 DOM 노드 (`chartSameNode: true`, 닫은 뒤에도 true)
- 주문 시트는 **끌어서 안 닫힌다**(`lockDrag`) — 수량 입력 중 미끄러지면 처음부터다.
  손잡이도 끌 수 있는 시트에만 보인다 (못 끄는데 손잡이가 있으면 거짓말)
- Kill Switch는 시트 안에 숨기지 않고 상단에 `STOP`으로 상주
- 호가에서 가격을 누르면 주문 시트로 갈아 끼우고 지정가에 채운다

### 디자인 토큰 정리 (`theme.ts`)
기존 팔레트가 촌스러웠던 이유 둘을 고쳤다:
1. 배경이 파란 네이비(#060B14)라 초록/빨강 손익색과 싸웠다 → 거의 무채색 먹색(#0A0B0D)
2. 8~10px 글씨 → 최소 10px, 본문 11~12px. 밀도는 글자가 아니라 여백·구분선에서 얻는다
- 경계선을 실선 대신 흰색 알파(.07) — 선이 보이면 화면이 조각나 보인다
- 색은 손익·경고·강조 셋만. 숫자는 전부 tabular-nums 고정폭
- 주문/Kill Switch 최소 46px 유지, 호가 행은 촘촘하게 (반대 방향으로 설계)

### 스트림 공유 (`useBinanceStream` 재작성)
훅을 부르는 곳마다 소켓을 새로 열고 있었다. 터미널만 해도 상단 가격·호가·주문
셋이 같은 심볼을 봐서 `depth20@100ms`가 세 벌 들어왔다 — 모바일에서는 그게 그대로
배터리와 데이터다. 심볼별 허브를 모듈 수준에 두고 `useSyncExternalStore`로 나눠 쓴다.
마지막 구독자가 떠나도 10초는 유지한다 (시트 여닫을 때마다 재연결하면 더 비싸다).
**검증**: 심볼을 SOL로 바꾸고 주문 시트까지 연 뒤 새로 열린 소켓 = **1개**.

**아직 안 한 것**: 차트 위 TP·SL 드래그, 다중 모니터 팝아웃, 단축키·명령 팔레트,
워크스페이스 여러 벌 저장, 체결 내역 탭(거래소 조회 추가 필요)

## PC 터미널 UI (원본 요청 명세)

기존 모바일 화면은 `모바일 모드`로 남기고, 새 화면을 `TRAIGO Pro 터미널`로 분리한다.
PC를 먼저 만들고, 모바일은 같은 컴포넌트를 다른 동선으로 재배치한다 (축소가 아님).

1920×1080 기준 배치:
```
좌 18%(시장·AI·뉴스·일정) | 중앙 55%(차트) | 우 27%(호가·주문·레버리지)
하단 25~30%: 포지션 | 미체결 | 체결 | 손익 | 전략 | Kill Switch
상단: 종목·가격·계좌·연결상태만 얇게
```
- 좌측 AI·뉴스 갱신이 중앙 차트를 재렌더링하지 않게 상태 격리
- 차트 심볼과 주문 심볼을 하나의 전역 선택 상태로 동기화
- 패널 크기·배치·심볼·주문설정을 사용자별 저장, 새로고침 후 복원
- 좁아지면 4패널 → 3패널 → 모바일 자동 전환
- 실전/Testnet을 배경색으로 구분
- 주문 버튼과 Kill Switch는 크게 유지. 고밀도는 뉴스·로그·통계에만 적용
- 모바일: 하단 고정 [주문][포지션][호가][AI·뉴스] + 바텀시트, 가로모드는 2열

## 신뢰성 기반 (요청 확정, UI보다 우선순위 높음)

사용자가 꼽은 핵심 5가지:
1. [x] **거래소 ↔ 앱 실시간 상태 대조** — `lib/engine/stateReconcile.ts` (순수 함수, 테스트 13개)
   + `GET /api/reconcile/state`. 심각(포지션 유무·방향·마진모드·손절소실)은 신규 주문 차단,
   경고(수량·레버리지)와 정보(잔고)는 차단하지 않는다. 허용 오차 수량 0.5%·잔고 1%.
   주문 경로(daily-ladder)에서 주문 직전 대조 후 409 차단, 화면에도 같은 근거를 배너로 표시.
   조회는 `reconcileCheck.ts`로 공용화 — 화면 판정과 주문 차단 근거가 항상 같다.
2. [x] **주문 전체 생명주기 + 중복 방지** — ``lib/engine/orderLifecycle.ts`` (상태 기계, 테스트 13개)
   13개 상태 + 허용 전이표. 종료 상태는 되돌릴 수 없고 건너뛰는 전이는 거부한다.
   orderExecutor의 update()가 이 검증을 통과한 전이만 기록한다 (DB의 INTENT/RECONCILED는 매핑).
   워커는 UNKNOWN(응답 없음)일 때 재시도하지 않고 멈춘다 — 중복 체결 방지.
   **남은 것**: 화면에 생명주기 표시, UNKNOWN 주문의 자동 조회 확정
3. [x] **연결 끊김·UNKNOWN 복구** — ``lib/engine/unknownResolver.ts`` (순수 함수, 테스트 11개)
   - **확정 규칙**: 거래소 조회 실패 / 반영 대기(5초) 내 / 멱등키 없음 / 포지션 못 읽음 /
     주문 없는데 포지션 변함 / 모르는 상태 문자열 → 전부 **확정하지 않는다**.
     멱등키 + 5초 경과 + 포지션 불변, 이 셋이 모두 맞아야 FAILED로 확정한다.
   - `reconcilePendingOrders`가 이 규칙을 쓴다. **기존 코드는 "조회에 안 보이면 즉시 FAILED"였다** —
     그러면 재시도가 열리고 그게 중복 체결이 됐다.
   - 근거 컬럼 추가: `pos_qty_before`(전송 직전 포지션), `resolve_attempts`, `needs_attention`
     → **마이그레이션 018 적용 필요** (아래 사용자 작업 참조). 적용 전에는 새 컬럼을 빼고
     다시 쓰는 폴백이 돈다 — 상태 기록이 통째로 날아가지 않게.
   - `assertStateConsistent`가 **미확정 주문이 1건이라도 있으면 신규 주문을 차단**한다.
   - exit-monitor cron이 청산 판단 **전에** 복구를 먼저 돌린다. 조회만 하고 재전송은 안 한다.
   - 호가 스트림에 **무응답 감시** 추가: 8초 조용하면 `⚠ 멈춤 N초` 표시, 25초면 강제 재연결.
     연결은 열려 있는데 데이터만 안 오는 경우 onclose가 안 와서 기존 재연결이 안 돌았다
     (실제로 겪은 문제다 — ticker 스트림이 정확히 이랬다).
4. [x] **데이터 출처·지연시간 표시** — ``lib/engine/dataQuality.ts`` (순수 함수, 테스트 12개)
   6가지 성격을 **서로 다른 모양**으로 구분한다 (색만 다르면 빠르게 훑을 때 안 보인다):
   `● 실시간(WS)` `○ 주기수신(REST)·계산값·AI추정` `◌ 모의(점선)` `⚠ 멈춤·수신없음`
   - 나이 기준을 값마다 다르게 잡는다. 호가는 100ms 주기, 변동률은 5초 주기다.
     하나로 묶으면 둘 중 하나는 반드시 거짓말이 된다.
   - 모의·추정은 아무리 새것이어도 `tradeable=false`다. 나이만 보면 그 구분이 사라진다.
   - "수신 없음"과 "멈춤"을 다른 상태로 둔다.
   - 매매 화면 배선: 호가 · 현재가 · 24h변동률 · 호가잔량 · 상단 대표가격
   **이 작업 중 드러난 실제 버그** (같이 고침): `useLivePrices`가 `action=coin`을 불렀는데
   그 응답은 코인 하나짜리 객체라 `fetchPrices`가 배열로 못 읽고 항상 빈 목록을 돌려줬다.
   결과적으로 **화면의 모든 종목 가격이 ASSETS 기본값을 난수보행한 값**이었고,
   상단 MOCK 배지만이 유일한 단서였다. BTC 헤더 ₩94,096,181 vs 호가창 63,3xx USDT(≈₩88M)로
   같은 화면에 두 값이 따로 놀았다. `all_crypto`로 바꿔 해결 — 이제 LIVE, 두 값이 일치한다.
5. [x] **단계별 운영 모드** — ``lib/engine/operatingMode.ts`` (순수 함수, 테스트 20개)
   `UI_DEMO → PAPER → TESTNET → SHADOW_LIVE → LIVE_SMALL → LIVE_LIMITED`
   - **한 칸씩만** 오른다. 건너뛰면 그 칸에서 얻었어야 할 증거가 통째로 빈다.
   - **하향은 언제나 허용**. 안전 밸브에 조건을 달면 정작 급할 때 못 내려간다.
   - 승급 조건: 표본 수 + 서로 다른 날짜 수 + 미확정 주문 0 + 심각 불일치 0
     (하루에 여러 건 넣어 기간을 채울 수 없다)
   - **Shadow Live**: 실계좌 시세로 판단은 전부 하되 주문만 안 보내고 `live_orders`에
     `SH` 접두사 + `status=INTENT`로 기록한다. 기록 안 하면 그냥 꺼둔 것과 같다.
     `status`가 SENT로 안 가므로 복구 대조(3번)가 이 행을 건드리지 않는다.
   - `LIVE_SMALL`은 $100 상한 **강제** + 건마다 `confirm:true` 필요(없으면 409).
   - 모르는 값은 `UI_DEMO`로 떨어진다. 예전 `LIVE`는 `LIVE_SMALL`로 옮겼다.
   - `GET /api/autotrade/mode` — 현재 단계·증거·다음 단계 가능 여부
   **로컬에서 실제로 확인**: `LIVE`/`LIVE_LIMITED` → 403(실거래 잠금),
   `REAL_MONEY_NOW`(오타 흉내) → `UI_DEMO`로 떨어짐, `SHADOW_LIVE`/`TESTNET` → 정상 판단 수행.
   **미확인**: 주문 승인이 나야 도달하는 `RECORD` 기록 경로 (그날 전략이 진입을 안 함)

성능: 호가·차트는 Canvas/WebGL 검토, 수신 200ms 쓰로틀링, 긴 내역 가상 스크롤,
보이지 않는 패널 갱신 축소, 심볼 변경 시 이전 요청 취소

보안: API 키 등록 화면에 **Worker 고정 IP 화이트리스팅 안내**, Secret은 등록 후 재표시 금지,
권한 분리(보기전용/모의/Testnet/실전수동/자동/관리자)

기타: 단축키·명령 팔레트, 다중 모니터 팝아웃, 워크스페이스 저장, 주문 초안,
이벤트 타임라인, 보안 화면, 법적 고지

## 검증 명령

```bash
npm test          # 유닛 테스트 897개 통과 / 0 실패 (테스트 파일 44개)
npm run typecheck # 타입 에러 80건
npm run build     # 프로덕션 빌드
```

2026-07-30 실행 결과다. `npm test`는 `node_modules`가 있어야 돈다 —
없으면 tsc를 못 찾고 "먼저 npm install을 실행하세요"로 멈춘다.

타입 에러 80건은 `next.config.js`의 `ignoreBuildErrors: true`가 덮고 있어서
빌드가 통과한다. 통과한다는 것이 맞다는 뜻은 아니다.

## 시장 분리 · 전략 계층 (완료)

`/terminal` 안에서 현물 / USDⓈ-M / COIN-M을 전환한다. 주문 경로·지갑·전략이 전부 분리돼 있다.

| 계층 | 파일 | 핵심 규칙 |
|---|---|---|
| 시장 유형 | `lib/markets/marketType.ts` | 없는 것은 0이 아니라 **없다**. 모르는 값은 `null` |
| 통합 자산 | `lib/markets/wallets.ts` | 현물 잔고를 선물 증거금에 **더하지 않는다** |
| 원가 | `lib/markets/costBasis.ts` | 코인으로 낸 수수료만큼 보유가 준다 |
| COIN-M | `lib/markets/coinM.ts` | 수량 단위가 **계약**. 계약 크기를 추측하지 않는다 |
| 전략 장부 | `lib/strategies/ledger.ts` | 자기 귀속분까지만 청산. 차이는 **미귀속**으로 |
| 현물 전략 10종 | `lib/strategies/spotStrategies.ts` | 의도 타입에 레버리지 필드가 **없다** |
| 현물 주문 도구 | `lib/strategies/spotOrderPlan.ts` | `guardedBy`로 거래소/앱 보호를 가른다 |
| 감시 루프 | `api/watch/tick` + GitHub Actions 15분 | **확인 주기를 화면에 그대로 적는다** |
| 결합 전략 6종 | `lib/strategies/combined.ts` | 반쪽 실행 시 **지금 상태**를 말한다 |

주문 경로는 시장마다 하나다 — `spot/order` · `futures/order` · `coinm/order`.
확인 토큰도 각각 다르다(`SPOT_` / `LIVE_` / `COINM_`). 같으면 엔드포인트만 바꿔도 통과한다.

**결합 전략 실데이터 확인** (BTCUSDT 63,523 · 펀딩 0.0001% · RSI 42.2):
- 증거금 1,000인데 헤지에 31,761 필요 → **차단** ("일부만 걸리면 헤지된 줄 알고 더 위험해집니다")
- 이미 숏 0.6 → 추가 0.4만 (기존 반영)
- 선물 다리 소실 → `BROKEN` "전액 노출 상태입니다"
- 한쪽 조회 실패 → `UNKNOWN` "정상이라는 뜻이 아닙니다"

## 정보 계층 — 뉴스 · AI · 캘린더 (완료)

이 문서가 한동안 놓치고 있던 부분이다. 시장 분리 이후 커밋 14개가 여기에 있다.

### 뉴스 파이프라인

수집과 분석을 **나눠 둔다**. 수집은 뉴스 API를, 분석은 AI API를 쓴다. 요금 체계도
실패 방식도 다르다. 붙여 두면 AI가 429를 뱉을 때 수집까지 죽고, 다음 회차에 원문을
처음부터 다시 받는다. 원문은 사실이라 한 번만 받으면 된다.

| 계층 | 파일 | 핵심 규칙 |
|---|---|---|
| 응답 검증 | `lib/news/schema.ts` | 검증을 통과 못 한 AI 응답은 흘리지 않는다. `uncertain`은 별도 값 |
| 수집·중복제거 | `lib/news/collect.ts` | 같은 기사에 AI를 두 번 부르지 않는다 (`contentHash`) |
| 기사 1건 분석 | `lib/news/analyzeOne.ts` | fetch를 주입받는다 — 키 없이도 동작을 검증할 수 있게 |
| 분석 대상 선정 | `lib/news/enrichPlan.ts` | **실패한 기사를 하루 96번 다시 부르지 않는다** |
| 합의 판정 | `lib/news/consensus.ts` | **갈렸을 때 평균을 내지 않는다** |
| 저장 | `lib/news/store.ts` | 원문 URL·발행 시각은 원문에서 온다 |

합의에서 평균을 금지하는 이유: 상승 80%와 하락 80%를 평균하면 "80% 확신"이 된다.
실제로는 두 모델이 정반대를 말한 것이고, 그건 아무것도 모르는 상태다. 숫자가 나오니까
화면에서는 확신 있어 보이는데, 그게 이 기능에서 가장 위험한 실패다.

화면: `components/news/AiVerdict.tsx` · `ConsensusPanel.tsx` — 판단 보류를 지우지 않는다.
API: `api/news/stored` · `api/ai/news/analyze` · `api/ai/consensus`

### AI 비용·응답시간 관리센터

`lib/ai/pricing.ts` + `components/pages/AiUsagePage.tsx` + `api/ai/usage`

**모르는 모델을 0원으로 적지 않는다.** 단가표에 없는 모델을 0으로 두면 화면에
"오늘 $0.00"이 뜬다. 사용자는 그걸 '무료'로 읽지 '모른다'로 읽지 않는다. 실제로는 돈이
나가고 있는데 화면만 안전해 보이는 상태다. 결과에 '추정'과 '단가 기준일'을 같이 들고 나간다.

관리자 전용이다 (`f98becc`) — **메뉴를 숨기는 것은 잠금이 아니다.**
OpenAI 오류는 사유를 실어 나른다 (`a73fc15`) — 429만으로는 대응을 정할 수 없다.

### 경제 캘린더

`lib/calendar/normalize.ts` + `api/cron/calendar-sync`

- **시각을 확정 못 하면 저장하지 않는다.** 시간대를 잘못 읽으면 FOMC가 몇 시간 밀리고,
  화면에는 그냥 '오후 2시'로 보인다. 사용자는 그 시각에 맞춰 포지션을 정리한다
- **발표 전 예상치를 실제치 자리에 넣지 않는다** (`677ba79`에서 고침). 아직 안 나온
  숫자를 "CPI 3.2% 나왔다"로 적는 것은 뉴스가 아니라 거짓말이다. 화면이 출처를 말한다

### 데이터 정리 (retention)

`lib/maintenance/retention.ts` + `api/cron/cleanup`

삭제는 되돌릴 수 없는데 실수는 조용하다. `?days=0`을 한 번 잘못 넘기면 컷오프가 '지금'이
되어 테이블 전체가 지워지고, 응답에는 "정리 완료 12,483건"이라고 뜬다. 성공처럼
보인다는 게 제일 나쁘다. 세 겹으로 막는다 — 보관 기간 하한 · 삭제 개수 상한 ·
표 전체를 지우려 하면 멈춤. **세지 못했으면 지우지 않는다.**

### 크론 배치

Vercel Hobby는 Cron이 하루 1회뿐이고 그 자리는 `exit-monitor`가 쓴다.
하루에 한 번 모으는 뉴스는 뉴스가 아니라서 나머지는 GitHub Actions로 옮겼다.

| 워크플로 | 주기 | 하는 일 |
|---|---|---|
| `vercel.json` cron | 하루 1회 00:00 UTC | `exit-monitor` (청산 감시) |
| `news-pipeline.yml` | 30분 | 뉴스 수집 → 캘린더 → AI 분석 → (UTC 18시에만) 정리 |
| `watch-tick.yml` | 15분 | 트레일링·예약 감시 |
| `exit-monitor.yml` | — | 청산 감시 백업 경로 |

뉴스 파이프라인은 **503(키 없음)만 실패로 표시한다.** 기사 하나가 이상해서 분석이 안
되는 것은 흔한 일이고, 그때마다 빨간 표시가 뜨면 진짜 고장을 놓친다.

### 터미널 · 내비 · 테마

| 작업 | 파일 | 핵심 |
|---|---|---|
| 선물 주문판 | 바이낸스 배치로 재구성 | **방향 토글을 없앤다** (매수/매도 버튼이 곧 방향) |
| 포지션 카드 | `lib/markets/positionView.ts` | 9열 표를 버리고 **0과 '없음'을 구분한다** |
| 시장가 청산 | 포지션 카드에서 직접 | 방향을 테스트로 못 박는다 |
| 오버레이 순서 | `lib/nav/overlayStack.ts` | 뒤로가기는 **맨 위 하나만** 닫는다 |
| 주소 동기화 | `app/page.tsx` | 주소창과 화면을 앱 전체에서 일치 |
| 테마 | `lib/theme/themeMode.ts` · `colors.ts` | 앱과 터미널이 한 팔레트. 밝음/어두움/시간 |

`positionView.ts`를 컴포넌트 밖으로 뺀 이유: 틀리면 안 되는 계산이 셋 있다 —
청산가가 0일 때, 증거금을 거래소가 안 줄 때, 수익률의 분모가 0일 때.
셋 다 화면에서는 그럴듯한 숫자로 보여서 눈으로는 틀린 걸 알 수 없다.

### 이 시기에 같이 고친 실제 버그

- `d6052a9` 가격 없는 종목이 **₩0으로** 뜨던 것 — 0원은 시세가 아니라 고장이다
- `1edf566` **'live'라고 적으면서 지어낸 수치를 섞던 것** — 시간대·출처 점검
- `5b28524` 더보기 메뉴 회귀 (위 '미처리 UI' 참조)

## 빠른 명령 팔레트 (Ctrl/Cmd+K) — 1차 완료

한글 **초성 검색** 포함. 테스트 54개.

### 왜 이것부터인가

팔레트와 전역 단축키는 아키텍처 레벨에서 같은 기능이다 — 둘 다 "실행 가능한 행동의
목록"이 필요하다. 레지스트리를 먼저 만들면 팔레트는 그 위의 **뷰**, 단축키는 **키맵**이
된다. 단축키를 먼저 만들면 `Shift+B → 매수`를 핸들러에 박게 되고 나중에 다 뜯는다.

그리고 `MENU`(lib/menuItems.tsx)가 이미 레지스트리의 80%였다 — 31개 항목에 검색
키워드(`kw`)까지 들어 있었다. 없던 것은 실행 방법과 위험 등급 둘뿐이다.

| 파일 | 역할 |
|---|---|
| `lib/commands/types.ts` | 커맨드 모양 + **위험 등급**. import가 없다 |
| `lib/commands/hangul.ts` | 초성 분해·비교 (순수) |
| `lib/commands/match.ts` | 점수·순위 (순수) |
| `lib/commands/registry.ts` | MENU → 커맨드 변환 + 추가 커맨드 (순수) |
| `lib/commands/useCommandHotkey.ts` | Ctrl/Cmd+K 리스너만 |
| `components/CommandPalette.tsx` | 뷰. 판단을 다시 하지 않는다 |

### 위험 등급을 창구가 아니라 커맨드에 둔다

```
safe      화면 이동·검색·테마 → 즉시
guarded   위험을 늘리는 것(주문)  → 확인 한 단계 (Enter 두 번)
reducing  위험을 줄이는 것(킬스위치) → 즉시
```

`reducing`을 따로 둔 이유: "위험한 건 다 확인"으로 묶으면 정작 급할 때 킬스위치 앞에
확인창이 뜬다. 그 0.5초가 이 앱에서 가장 비싼 0.5초다. 반대로 매수를 즉시 실행하면
오타 한 번이 포지션이 된다. **방향이 다르면 처리도 달라야 한다.**

등급을 키 핸들러가 아니라 커맨드에 두는 이유: 창구가 앞으로 여럿(팔레트·단축키·
플로팅 버튼)이 되는데, 창구마다 판단하면 그중 하나는 반드시 빼먹는다.

**지금 실려 있는 커맨드는 전부 `safe`다.** 킬스위치를 팔레트에서 바로 발동시키지
않았다 — `/api/risk/kill-switch/trigger`가 `connectionId`를 요구하고, 어느 연결을
끊을지 팔레트는 모른다. 그런데도 `reducing`을 달면 배지는 "즉시 실행"인데 실제로는
화면만 열린다. 급할 때 그 배지를 믿고 Enter를 친 사람은 자동매매가 멈춘 줄 알고 손을
뗀다. 테스트가 "실린 커맨드는 전부 safe"를 못 박아서, 나중에 위험 동작을 배선할 때
**먼저 실패해서 확인 흐름도 같이 붙이라고 알려준다.**

### 초성 매칭에서 정한 것

- **호환 자모**(U+3131 `ㄱ`)를 쓴다. 음절을 분해해 얻는 초성(U+1100 `ᄀ`)과 코드가
  다른데 눈으로는 같아 보인다. 섞으면 "왜 검색이 안 되지"가 된다
- 호환 자모 블록에 `ㄳ`·`ㄵ` 같은 겹받침이 끼어 있어 **코드 산술로 초성 번호를 구할 수
  없다.** 초성 19자를 순서대로 적어 인덱스를 쓴다 (쌍자음 테스트로 못 박음)
- **건너뛰기 없음.** `ㄱㅋ`는 경제캘린더에 안 맞는다 (`ㄱㅈㅋ`가 맞다). 허용하면
  `ㅁㅁ`이 거의 전부에 맞아 결과가 목록 전체가 된다 — 그건 검색이 아니다
- `ㄱ`은 `ㄲ`에 맞지 않는다. 느슨하게 하면 한 글자 질의 결과가 두 배가 되고 순위가
  무의미해진다
- **모음만 치면 결과 0건**이고 던지지 않는다
- 초성으로 맞은 것은 계층을 **낮춰** 기록한다. 그러지 않으면 `ㅈㄷ`와 `자동`이 같은
  점수를 받아, 정확히 친 사람이 초성만 친 사람과 같은 대우를 받는다
- kw·한글 이름에도 초성이 통한다 — 라벨에만 통하면 한글 사용자에게 kw는 무용지물이다

순위는 사용 빈도를 학습하지 않는다. 같은 입력이 어제와 다른 결과를 내면 손이 기억한
순서가 매번 틀린다. 첫 줄이 중요한 이유는 사용자가 `Ctrl+K → 타이핑 → Enter`를
습관으로 만들기 때문이다.

### 실제 MENU 29개 + 종목으로 확인

| 질의 | 첫 결과 |
|---|---|
| `ㄱㅈㅋ` | 경제캘린더 |
| `ㅂㅌㅅㅌ` | 백테스트 |
| `ㅍㅌㅍㄹㅇ` | 포트폴리오 |
| `ㅁㅁ` | 매매하기 (모의매매·자동매매보다 앞에서 맞았다) |
| `ㅋㅅ` | 리스크 관리 — 라벨에 '킬스위치'가 없는데 kw에서 잡혔다 |
| `fomc` | 경제캘린더 (kw) |
| `ㅅㅅㅈㅈ` | 005930 (삼성전자) |
| `ㅇㅂㄷㅇ` | NVDA (엔비디아) |

**이 확인 중에 내가 만든 버그를 찾았다**: `ㅋㅅ` 결과에 '리스크 관리'가 두 줄로 나왔다.
MENU에 이미 있는 항목을 EXTRA_COMMANDS에 또 적었기 때문이다 — 이 파일 맨 위에 적어 둔
"메뉴를 다시 적지 않는다"를 내가 어긴 것이었다. 픽스처 테스트만으로는 안 보였고,
실제 MENU로 돌려봐야 보였다.

### 남은 것

- 종목 결과가 `useLivePrices`의 목록에 한정된다. 거래소 전체 300개+는 `SymbolSearch`가
  따로 갖고 있어 아직 합치지 않았다
- 최근 방문·즐겨찾기를 팔레트 상단에 (즐겨찾기 자체는 이미 있다)
- 색맹 모드 · 패널 팝아웃 (팝아웃은 차트 iframe 동일 노드 보장과 충돌한다 — 주의)

## 조용히 쌓이던 주문 (완료)

`lib/jobs/executorHealth.ts`(순수, 테스트 17개) + `queueGuard.ts` + 세 라우트 + 워커.

### 무엇이 문제였나

`/api/binance/futures/order`는 거래소를 직접 부르지 않는다. jobs 큐에 PLACE_ORDER를
적재하고 `{ ok: true, queued: true }`를 돌려준다. 실행자는 **Railway Worker 하나뿐**인데,
그 워커는 이 문서 위쪽에 적힌 대로 Binance IP 지역 차단으로 쓰지 않고 있다.

즉 이 경로로 넣은 주문은 **큐에 쌓이기만 하고 실행되지 않았다.** 그런데 응답이
`ok: true`라 화면은 "주문됨"으로 그렸다. 사용자는 포지션이 열렸다고 믿고 손을 뗐다.

**성공처럼 보이는 실패** — 이 앱에서 가장 위험한 종류다.

범위도 좁지 않았다. 이 경로를 부르는 화면이 **7곳**이고(`OrderPane`·`BottomDock`·
`AutoTradeEngine`·`TradingPage` 4곳), `marketType.ts`가 USDⓈ-M의 `orderEndpoint`로
선언하고 있다. 웹훅 둘(`webhook/signal`·`webhook/tradingview`)도 같은 큐를 쓴다.
`src/app/api` 어디에도 큐를 비우는 코드가 없다.

### 두 가지를 정했다

**1. 실행자가 없으면 적재하지 않고 503으로 거절한다.**
적재해 두면 지뢰가 된다 — 워커가 몇 시간 뒤 살아나면 그때 시세로 주문이 나가고,
사용자는 그 주문을 이미 잊었다. 응답에 "큐에 넣지 않았다"를 명시한다.

`degraded`(25초 지연)는 허용한다. 느린 것과 죽은 것은 다르고, 25초 지연으로 매매를
막으면 정상 운영 중에도 자주 걸린다. `stopped`·`absent`·**`unknown`**은 막는다 —
조회 실패를 '정상'으로 볼 수 없다.

**2. 적재된 주문에 만료 시각을 새긴다 (2분).**
워커가 돌아왔을 때 오래된 주문을 실행하지 않도록. 워커 쪽에도 검사를 넣었다.
**만료 시각이 없는 주문 — 이 보호 이전에 쌓인 것 — 도 실행하지 않는다.** 언제 만든
것인지 알 수 없는 주문을 내보내는 쪽이 더 위험하다.

만료는 **주문 계열에만** 적용한다. 킬스위치·전량청산은 늦게라도 실행되는 것이 맞다 —
위험을 줄이는 방향이다.

TTL을 2분으로 잡은 이유: 수동 주문은 사용자가 화면을 보며 누른 것이다. 2분이면 시세가
이미 달라졌고 그 가격을 의도했다고 볼 수 없다. 길면 "왜 아까 취소한 주문이 나갔지"가
되고, 짧으면 정상적인 큐 지연에도 실패한다.

마이그레이션을 새로 만들지 않고 `payload`(JSON)에 담았다 — 이미 적용 안 된 것이
세 개(018·019·020)라, 네 번째를 더하면 그것이 적용될 때까지 이 보호가 동작하지 않는다.

### 판정을 한 곳으로

`/api/worker/status`(화면)가 임계값을 자기 코드에 갖고 있었다. 주문 경로가 따로 판단하면
화면은 '정상'인데 주문은 막히는(또는 그 반대) 상태가 생긴다. `judgeExecutor` 하나를
둘이 같이 쓰고, 응답에 `canQueue`를 실어 화면이 직접 계산하지 않게 했다.

### 실제로 확인한 것

| 하트비트 | 결과 |
|---|---|
| 3초 전 | ✅ 적재 [정상] |
| 30초 전 | ✅ 적재 [지연] — 느린 것과 죽은 것은 다르다 |
| 5분 전 | 🚫 거절 [중단(heartbeat 끊김)] |
| 스스로 `stopped` 보고 | 🚫 거절 — 나이와 무관 |
| 행 없음 | 🚫 거절 [워커 없음] |
| **DB 조회 실패** | 🚫 거절 [상태 확인 실패] |
| `last_seen` 깨짐 | 🚫 거절 |

| 워커가 집은 시점 | 결과 |
|---|---|
| 119초 뒤 | ✅ 실행 |
| 3분 뒤 | 🚫 "60초 지난 주문입니다" |
| 3시간 뒤 | 🚫 "10680초 지난 주문입니다" |
| 만료 시각 없음 | 🚫 "언제 만들어진 것인지 알 수 없어" |

### 수동 선물 주문을 Vercel에서 직접 실행 (완료)

`futures/order`가 큐를 쓰지 않고 `executeOrder`를 직접 부른다. `daily-ladder`와 같은
경로다 — 생명주기 기록·멱등 키·ISOLATED 강제·손절 부착·UNKNOWN 처리가 전부 따라온다.
워커 경로에는 그게 절반만 있었다.

관문 순서: 확인 토큰 → 파라미터 검증 → 연결·출금권한 → 킬스위치 → **거래 전 점검**
→ **수동 계획 검사** → `executeOrder`.

큐는 남겨 뒀다 — 웹훅 둘(`signal`·`tradingview`)이 아직 쓴다. 그쪽은 실행자가 없으면
적재하지 않는다.

#### `approved`를 그냥 true로 두지 않는다

`executeOrder`는 `plan.approved`만 보고 주문을 보낸다. 그 필드는 "riskManager를
통과했다"는 뜻이다. 수동 주문에 조건 없이 true를 박으면 **위험 계층을 우회하는 통로**가
생기고, 그 통로는 코드에서 보이지 않는다.

그래서 `lib/engine/manualPlan.ts`(순수, 테스트 26개)가 검사를 통과해야만 approved를 준다:

| 검사 | 거부 사유 |
|---|---|
| 수량 ≤ 0 / NaN | 값이 성립하지 않는다 |
| 배율 < 1 또는 > 100 | 이 경로의 상한 |
| **진입에 손절 없음** | 손절 없는 진입은 허용하지 않는다 |
| 손절 방향 반대 | "즉시 체결됩니다" |

`executeOrder`는 손절이 없어도 주문을 보낸다(자동매매는 `exitPlan`으로 따로 건다).
수동 진입에 그 관용을 두면 **손절 없는 포지션이 이 경로로만 열린다** — 가장 흔한
계좌 소멸 경로다.

**수량을 다시 계산하지 않는다.** 수동 주문은 사용자가 정한 것이고, 조용히 바꾸면
화면에 적은 숫자와 나간 주문이 달라진다.

#### `executeOrder`에 있던 하드코딩 셋

1. **`type: 'MARKET'` 하드코딩.** 지정가 주문을 그대로 넘기면 **조용히 시장가로**
   나간다. 지정가를 쓰는 이유가 '그 가격에만 사겠다'는 것이므로 주문을 무시하는 것과
   같다. `orderType`·`limitPrice`를 받고, 지정가에 가격이 없으면 전송 전에 거부한다
2. **`order_type: 'MARKET'` 기록 하드코딩.** 지정가를 MARKET으로 적으면 나중에
   내역을 볼 때 왜 그 가격에 체결됐는지 설명이 안 된다
3. **`reduce_only`를 아예 기록하지 않았다** — 아래 참조

기본값은 전부 기존 동작이라 `daily-ladder`는 달라지지 않는다.

#### 방향 왕복 — 틀리면 청산이 두 배가 된다

`manualPlan`이 청산 주문의 방향을 뒤집어 `plan.side`(어느 포지션인가)로 적고,
`executeOrder`가 `reduceOnly`를 보고 **다시 뒤집어** 거래소로 보낸다. 두 곳 중 한 곳만
바뀌면 청산 주문이 같은 방향 진입이 되어 포지션이 두 배가 된다 — 100배 격리에서는
즉시 청산이다. 네 조합(BUY/SELL × 진입/청산) 모두 들어온 방향과 나가는 방향이 같은지
테스트로 못 박았다.

#### 이 작업에서 드러난 잠재 버그

**`reduce_only`를 기록하지 않아 청산이 반대 포지션으로 잡혔다.**
`live_orders.reduce_only`는 마이그레이션 012부터 있는데 `executeOrder`가 채우지
않았다. 그러면 `reconcileCheck`가 그 행을 **반대 방향 진입**으로 읽는다 — 롱을 닫은
SELL 기록이 숏 포지션이 되고, 거래소에 그런 포지션이 없으니 심각 불일치가 되어
**이후 주문이 전부 막힌다.**

`daily-ladder`는 진입만 하므로 드러나지 않았다. 수동 청산이 이 경로를 타면서 실제
문제가 된다. 둘 다 고쳤다 — 기록하고, 대조에서 제외한다. 컬럼이 없는 스키마에서
select가 실패하면 앱 포지션이 통째로 사라지므로 컬럼을 빼고 다시 읽는 폴백을 뒀다.

#### 멱등 키

`MF` + 분(minute) + 심볼 + 방향 + 수량. 같은 주문을 **같은 분에 두 번 누르면** 같은 키가
되어 거래소가 두 번째를 거부한다 — 손이 떨려 두 번 누른 것과 정말 두 번 주문하려는
것을 구분할 수 없으므로 실수 쪽을 막는다. 1분 뒤에는 다른 키가 되므로 의도적인 반복은
가능하다.

#### 응답

`UNKNOWN`은 **502**로 돌려준다. 200이면 화면이 성공으로 그리고, 그게 중복 주문을 부른다.
`queued: false`를 명시해 화면이 예전 응답 모양을 보고 "적재됨"으로 그리지 않게 했다.

### 웹훅 둘도 직접 실행 (완료)

**`webhook/signal`** — 이미 직접 실행 경로가 있었다. 큐는 폴백이었는데, 그 폴백이
걸리는 조건이 문제였다: **위험 계산이 실행 자체에 실패했을 때**(`plan`이 undefined).

이 파일은 바로 위에 "Risk Manager가 거부하면 큐에 넣지 않는다 — 주문 권한의 마지막 문"
이라고 적어 두고, **거부보다 정보가 더 없는 상태**(실행조차 못 함)만 큐로 빠져나가
워커가 대신 크기를 정하게 되어 있었다. 폴백을 없애고 202 + `risk_plan_unavailable`로
돌려준다.

**`webhook/tradingview`** — 직접 실행으로 옮겼다. 수동 주문과 같은 경로다
(거래 전 점검 → `buildManualPlan` → `executeOrder`).

#### tradingview에서 드러난 것 셋

1. **`reduceOnly`가 주문에 넘어가지 않았다.** 킬스위치 판단에는 `body.reduceOnly`를
   쓰면서(신규 진입만 차단) 큐 payload에는 넣지 않았다. `closeLong` 신호가
   **신규 진입으로** 나갈 수 있었다. 이제 `orderType`(closeLong 등)·`action`·
   `reduceOnly` 셋 중 하나라도 청산이면 EXIT로 본다
2. **손절 없이 주문했다.** 알림 하나로 보호 없는 포지션이 열렸다. 이제 진입에
   `stopLossPct` 또는 `stopPrice`가 없으면 400으로 거부하고, 응답에 무엇을 넣어야
   하는지 적어 준다. **기존 TradingView 알림을 고쳐야 한다**
3. **죽은 환율 계산.** `(amount/1375) / (px/1375)`로 원-달러 추정치가 두 번 들어가
   서로 상쇄되고 있었다. 결과는 `amount/px`와 같지만, 읽는 사람은 통화 변환이
   일어난다고 믿는다. 실제 가정은 'amount와 price가 같은 통화'뿐이라 그대로 적었다

#### 응답

둘 다 `queued: false`를 명시한다. 예전에는 `status: 'queued'` / `queued: true`를
돌려줬고 화면·알림이 "처리 예정"으로 읽었다 — 실제로는 아무도 처리하지 않았다.
`UNKNOWN`은 502다. TradingView는 실패를 재발사할 수 있고, 200을 주면 그 재발사가
중복 체결이 된다. 15초 중복 창(`claimSignal`)과 `clientOrderId` 멱등이 그걸 막는다.

### 이제 `PLACE_ORDER`는 어디서도 큐를 쓰지 않는다

`grep PLACE_ORDER src/app` → 0건. 네 주문 경로 전부 Vercel에서 직접 실행한다.

### 킬스위치와 청산 경로 7개도 직접 실행 (완료)

**킬스위치가 문을 잠그지만 안에 있는 것을 꺼내지 않고 있었다.** 누르면 DB의
`active=true`가 켜져 신규 주문은 막혔지만, 미체결 취소와 포지션 종료는 큐에
적재되고 그 큐를 비우는 워커가 없었다. 급할 때 누른 사람은 정리됐다고 믿고 손을
뗀다 — 이 저장소에서 가장 위험한 실패였다.

실제로 꺼내는 `executeKillActions`는 `lib/risk/killSwitch.ts`에 있었고 그 라우트가
**이미 import까지 해 두었는데 부르지 않았다.**

| 라우트 | 이제 |
|---|---|
| `risk/kill-switch/trigger` | `executeKillActions` 직접 호출 |
| `risk/kill-switch/status` (자동 발동) | 같음 |
| `binance/futures/close-all` | 취소 → 종료 순서로 직접 |
| `binance/futures/cancel-all` | 직접 (최대 3회 재시도) |
| `binance/futures/close-position` | `closePositionPercent` 신규 |
| `binance/futures/tpsl` | 직접 (포지션·방향 확인 후) |
| `telegram/callback` | 취소 → 종료 직접 |

**`grep enqueueJob src/app` → 0건.** 앱은 더 이상 jobs 큐를 쓰지 않는다.

#### 실패를 성공에 섞지 않는다

- 킬스위치: 취소·종료가 실패하면 `ok: false` + 502. "킬스위치는 켜졌지만(신규 주문
  차단) 취소·종료가 완료되지 않았습니다"라고 적는다. `ok: true`로 돌려주면 화면이
  "정리됨"으로 그리고 사용자는 거래소를 확인하지 않는다
- `close-all`: 취소 실패는 종료를 막지 않되 응답에 그대로 실어 보낸다
- `tpsl`: 손절 실패를 성공에 섞지 않는다. 익절만 걸리고 손절이 실패했는데
  `ok: true`를 주면 사용자는 보호가 걸렸다고 믿는다
- 전부 `queued: false`를 명시한다

#### 이 작업에서 드러난 것 셋

**1. `cancelOpenTPSL`이 분할 익절까지 취소했다.** 감사 지적 6번("TP/SL 수정이 타 주문
취소")의 수정이 `worker/src/binance.ts`에만 들어가 있었고 앱의 이 함수에는 오지
않았다. 지금까지 **호출자가 없어서** 드러나지 않았다. TP/SL 라우트를 직접 실행으로
옮기며 함께 고쳤다 — `closePosition=true`만 취소한다.

**2. `close-position`이 쓰지 않는 파라미터를 필수로 요구했다.** `quantity`를 필수로
받으면서 워커에는 `percent`만 넘겼다. 화면이 보낸 수량은 어디에도 쓰이지 않았다.

**3. 부분 청산 비율 검사에서 주석과 코드가 달랐다.** `Number(percent) || 100`으로
두고 "0이나 음수는 전량"이라 적었는데, 실제로는 음수가 1%로 조여졌다. 어느 쪽이든
**잘못된 입력으로 실제 주문을 낸다** — 100%면 전량이 날아가고 1%면 요청하지 않은
거래가 나간다. 이제 0·음수·NaN은 **거래하지 않는다**(테스트로 고정).

#### 공용 자격증명 로더

`lib/exchanges/loadCreds.ts` — 소유권·거래소·출금권한·복호화 검사를 한 곳에 모았다.
다섯 라우트가 같은 다섯 줄을 복사해 갖고 있었고, 하나만 빠뜨리면 그 경로에서 남의
연결로 주문하거나 출금 권한 키를 쓴다. 이 함수를 통과하지 못하면 키를 얻지 못하므로
검사를 건너뛰는 길이 없다.

`is_testnet`은 **명시적으로 false일 때만 실전**으로 본다. 모르는 값이 실전이 되면 안 된다.

### 남은 것

- `src/lib/jobs.ts`와 `worker/`는 남아 있다. 앱은 쓰지 않지만, 워커를 다시 쓸
  선택지를 없애지 않았다. 워커에는 만료 검사가 들어가 있다
- ~~Gate 거래소 경로~~ ✅ 처리 (아래 'Gate 거래소 경로' 절). 손절 API만 실계좌 미검증

## 거래 전 점검 (완료)

`lib/engine/preTradeChecklist.ts`(순수, 테스트 37개) + `preflight.ts`(수집) +
`POST /api/orders/preflight` + `components/PreTradeChecklist.tsx`.

### 검사가 없어서 만든 게 아니다

검사는 이미 다 있었다 — 운영 모드·상태 대조·위험 계산·하루 1회 제한·포지션 보호.
문제는 **흩어져 있다는 것**이었다. 주문 경로가 순서대로 부르고 각자 다른 모양의
실패를 돌려주므로, 화면은 "왜 주문이 안 나갔는지"를 한 곳에서 말할 수 없고 검사
하나를 빼먹었는지 아닌지 아무도 확인할 수 없었다. 이 모듈은 검사를 **다시 구현하지
않는다.** 결과를 받아 하나의 목록으로 만들고 통과 여부를 한 번에 판정한다.

**실제로 없던 검사는 하나였다: 시간 동기화.** `recvWindow=5000`만 있고 비교가 없었다.

### 핵심 규칙: 확인하지 못한 것은 통과가 아니다

체크리스트에서 가장 위험한 실패는 빨간 X가 아니다. **확인 못 한 항목을 초록으로
그리는 것**이다. 거래소 조회가 실패해 마진 모드를 못 읽었는데 "6/6 확인"이 뜨면
사용자는 점검이 끝났다고 믿고 주문을 넣는다. 확인 못 한 것을 초록으로 그리는
체크리스트는 없는 것보다 나쁘다 — 없으면 사람이 직접 보지만, 있으면 안 본다.

그래서 상태가 넷이다: `pass` / `fail` / `warn` / **`unknown`**. 필수 항목이
`unknown`이면 막는다. 수집 층(`preflight.ts`)은 조회 실패한 값을 **넣지 않는다** —
기본값으로 채우면 그 순간 점검이 거짓말을 시작한다.

### 검사 11개

| 항목 | fail이면 | 모르면 |
|---|---|---|
| 운영 모드 · 시계 오차 · 상태 대조 · 미확정 주문 | 차단 | 차단 |
| 마진 ISOLATED · 손절 부착 · 손절<청산 · 증거금 | 차단 | 차단 |
| 오늘 진입 이력 | 차단 | 통과 |
| 배율 일치 · 기존 포지션 | 알림만 | 통과 |

막을 것과 알릴 것을 섞지 않았다 — 섞으면 사용자는 곧 전부 무시한다. 기존 포지션이
있다는 것은 사실 전달이고, 추가 진입이 잘못이라고 단정할 수 없다.
테스트가 "막지 않는 항목은 모른다고 막지도 않는다"를 못 박아 기준이 엇갈리는 것을 막는다.

### 정한 것

- **시계 여유를 recvWindow의 60%로** 잡는다. recvWindow는 timestamp부터 거래소
  도착까지의 예산이고 왕복 지연이 그 안에서 소모된다. 오차가 4.9/5.0초면 실제로는
  매번 실패한다 — 경계에 붙은 값을 통과로 적으면 "가끔 실패하는" 상태가 되고,
  그게 원인을 찾기 가장 어려운 모양이다. 문구에 `-1021`을 적어 둔다
- **로컬 시각을 호출 직후에 찍는다.** 응답을 기다린 뒤 찍으면 왕복 지연이 그대로
  오차로 계산돼 시계가 정확해도 실패로 뜬다
- **청산 거리는 방향을 본다.** LONG은 손절이 청산보다 위, SHORT는 아래여야 한다.
  부호를 잘못 보면 가장 위험한 주문이 통과한다
- **Shadow Live는 `warn`.** 통과로만 적으면 사용자는 주문이 실제로 나갔다고 믿는다
- **포지션이 없으면 마진 모드는 `unknown`이다.** 여기서 'isolated'로 가정하면
  CROSS 계좌의 첫 주문이 그대로 나간다
- **`preflight`는 `openLadderGate`를 부르지 않는다.** 그 함수는 판정과 동시에
  오늘의 슬롯을 **예약**한다(동시 요청 방어). 점검하려고 부르면 점검만으로 하루치
  슬롯이 소모되고 정작 주문할 때 막힌다. 호출자가 알 때만 넘긴다
- 화면은 네 상태를 **모양으로도** 구분한다 (`✓ ✕ ! ?`). 색만 다르면 빠르게 훑을 때
  안 보이고 색맹 사용자에게는 구분이 없다 — `dataQuality`와 같은 규칙
- 차단됐을 때 **회색 버튼만 두지 않고 이유를 버튼 아래에 적는다.** 안 그러면
  사용자는 화면이 고장 났다고 생각한다
- 요약에 **확인 못 한 항목 수를 넣는다.** "9/11 통과"만 적으면 나머지 2개가 실패인지
  확인 불가인지 알 수 없고, 둘은 대응이 완전히 다르다

`PositionView`에 `liquidationPrice`를 실었다 — 대조에는 안 쓰지만 점검이 필요하고,
거래소 포지션을 읽는 곳이 한 곳이라 빼면 같은 조회를 두 번 하게 된다.

### 실제로 확인한 것

판정 조립을 시나리오별로 돌렸다:

| 상황 | 결과 |
|---|---|
| 전부 정상 | ✅ 11/11 통과 |
| **거래소 조회 실패** | 🚫 5개 항목이 차단 (전부 `?` — 하나도 초록이 아니다) |
| CROSS 계좌 | 🚫 마진 모드 차단 |
| 시계 8초 앞섬 | 🚫 "-1021로 거절합니다" |
| 손절보다 청산이 먼저 | 🚫 "손절은 작동할 기회가 없습니다" |
| 손절 없음 | 🚫 2개 차단 (부착 + 청산거리 unknown) |
| Shadow Live | ✅ 허용 + "거래소로 보내지 않습니다" |
| 배율 100배/의도 10배 + 기존 포지션 | ✅ 허용 + 알림 2개 |

라우트는 인증이 먼저 막는 것까지 확인(401). **전체 경로는 미확인** — 실제 Supabase
연결과 거래소 키가 필요하다.

### 주문 경로에 물렸다 (완료)

`daily-ladder`가 `executeOrder` **직전**에 `runChecklist`를 돌리고, 통과하지 못하면
409 `CHECKLIST_BLOCKED`로 막는다. 관문 순서:

```
모드 관문 → 다른 전략 보유 확인 → 상태 대조 → 손절·청산계획 계산
  → 거래 전 점검 ← 마지막 관문
  → executeOrder
```

**막히면 오늘 하루를 돌려준다** (`releaseReservation`). 슬롯을 소모하면 원인을 고친
뒤에도 내일까지 기다려야 한다.

성공 응답에도 `checklist`를 실어 보낸다. 막힐 때만 보여주면 점검이 돌았는지 알 수
없고, 통과했지만 확인 못 한 항목이 있었다는 사실도 사라진다.

**거래소를 다시 읽지 않는다.** `assertStateConsistent`가 방금 읽은 것을 `gather`로
돌려주게 바꿨다 — 안 그러면 주문 경로가 거래소를 두 번 읽고, 두 조회 사이에 상태가
바뀌면 점검과 차단이 서로 다른 사실을 본다. 추가 호출은 두 개뿐이다:
서버 시각(공개)과 이 심볼의 위험 정보.

### 배선하면서 실제로 걸린 것 — 모든 신규 진입이 막혔다

처음 물렸을 때 **첫 주문이 전부 차단**됐다. 원인:

- `getFuturesPositions`는 `positionAmt !== 0`으로 걸러낸다. 목록 화면에는 맞다
- 그런데 **신규 진입은 정의상 수량이 0**이라 그 심볼이 목록에 없다
- 그러면 `marginType`이 `unknown` → 필수 항목이라 차단 → 전략이 아예 못 돈다

마진 모드는 포지션이 아니라 **심볼별 계좌 설정**이라 수량 0에도 존재한다. 그 값을
읽는 `getSymbolPositionRisk(symbol)`를 추가했다 — `?symbol=`로 한 개만 받으므로
전체를 받아 거르는 것보다 싸다. 헤지 모드에서 같은 심볼에 두 줄이 오는 것도 처리한다.

`orderExecutor`가 ISOLATED를 설정하지만(214행) 그건 점검 **이후**라서, 점검 시점에
읽을 방법이 따로 필요했다.

`preflight.ts`도 같은 함수를 쓰게 맞췄다. 여기서 다른 방법으로 읽으면 미리 본
결과와 실제로 막히는 근거가 갈린다 — 상태 대조를 공용화한 것과 같은 이유다.

### 네 경로 전부에 물렸다 — 시장·의도별로 다르게

주문 경로마다 같은 검사를 물리면 안 된다. **현물에는 마진 모드·청산가·레버리지가
존재하지 않는다.** 선물 체크리스트를 그대로 물렸다가 현물 주문이 전부 막혔다.
없는 것을 `pass`로 적는 것도 답이 아니다 — 현물 주문에 "마진 모드 ISOLATED ✓"가
뜨면 확인한 것도 사실도 아니다. **해당 없는 검사는 목록에서 뺀다.**

`market`(SPOT/USDM/COINM) × `intent`(ENTRY/EXIT) 두 축으로 검사를 고른다:

| 경로 | 검사 | 차단 항목 |
|---|---|---|
| `daily-ladder` (USDM 진입, 하루제한) | 11 | 8 |
| `futures/order` (USDM 진입) | 10 | 8 |
| `futures/order` (USDM 청산) | 4 | 3 |
| `coinm/order` (COIN-M 진입) | 7 | 5 |
| `coinm/order` (COIN-M 청산) | 3 | 2 |
| `spot/order` (현물 매수·매도) | 2 | 2 |

**청산(EXIT)에 진입 검사를 물리면 나갈 수 없게 된다.** CROSS 포지션을 정리하려는데
"ISOLATED가 아닙니다"로 막히고, 손절을 안 붙였다고 막히고, 증거금이 부족하다고
막힌다 — 청산은 증거금을 **돌려주는** 동작이다. 커맨드 레지스트리의 `reducing`과
같은 생각이다.

### 물리면서 드러난 것 세 가지

**1. 현물 경로는 운영 모드 관문을 지나지 않았다.** `spotOrderExecutor`는 공매도·
보유·정밀도를 보지만 모드는 보지 않는다. 즉 `UI_DEMO`나 `PAPER` 모드에서도 실제
거래소로 현물 주문이 나갔다. 모드 관문이 현물 체크리스트의 주된 값어치다.

**2. COIN-M 경로는 손절을 아예 붙이지 않는다.** `STOP_ATTACHED`·
`LIQUIDATION_DISTANCE`를 COIN-M에 물리자 모든 COIN-M 주문이 차단됐다. 두 검사를
USDⓈ-M 전용으로 좁혔다 — **면제가 아니라 아직 못 고친 위험의 표시다.** 그 경로에
손절 부착을 넣으면 `markets`에 `'COINM'`을 더해야 하고, 테스트가 그때 뒤집힌다.

**3. 현물 증거금 검사는 껍데기가 된다.** 시장가+수량 주문은 체결가를 몰라 필요
금액을 계산할 수 없다. 추측해 채우면 `required: 0`으로 무조건 통과하는 초록 체크가
생긴다. 현물 자금 부족은 거래소가 주문을 통째로 거부하므로(선물처럼 일부 체결로
어중간한 포지션이 남지 않는다) 항목에서 뺐다.

### 호스트마다 시각을 따로 읽는다

`getFuturesServerTime`(fapi) · `getSpotServerTime`(api) · `getCoinMServerTime`(dapi).
하나로 쓰면 현물 주문의 시계를 선물 호스트에 물어보는 것이 된다. 테스트넷은 더
분명하다 — 현물은 `testnet.binance.vision`, 선물은 `demo-fapi`로 **서로 다른
거래소**다.

### 남은 것

- 화면 배선 (터미널 주문판에 `PreTradeChecklist` 붙이기). 컴포넌트는 있다
- `alreadyTradedToday`를 예약 없이 읽는 조회
- ~~**COIN-M 손절 부착**~~ ✅ 완료 (아래 'COIN-M 손절 부착' 절). 체크리스트 범위도 넓혔다
- ~~Gate 경로에는 안 물렸다~~ ✅ 물렸다. `preflight`·일일 사다리·TradingView 웹훅이
  거래소별로 갈라 읽는다 (아래 'Gate 거래소 경로' 절)
- ~~`futures/order`가 실행되지 않는 문제~~ → 아래 '조용히 쌓이던 주문' 절에서 처리

## 일일 최대 손실 자동 잠금 (완료)

`lib/risk/dailyLoss.ts`(순수, 테스트 20개) + `dailyLossCheck.ts`(수집) +
체크리스트 `DAILY_LOSS_LIMIT` + `api/risk/daily-limit` + 주문판 표시.

### 예전 것은 브라우저에만 있었다

`risk/guard.ts`가 오늘의 손익을 **localStorage에** 들고 있었다. 그래서:

- 브라우저 저장소를 지우면 한도가 0으로 리셋된다
- 다른 기기·다른 브라우저로 열면 오늘 얼마를 잃었는지 모른다
- **웹훅·크론·서버 라우트에는 아예 적용되지 않았다** — 지금 실제로 주문이
  나가는 경로가 전부 그쪽이다

즉 화면에는 '일일 손실 제한 켜짐'이라고 떠 있는데 주문은 그 제한과 무관하게
나가고 있었다. 이 절의 작업은 그 판정을 **거래소가 알려준 실현손익** 위에서
다시 세운 것이다.

### 정한 것

- **실현손익 + 수수료 + 펀딩을 모두 센다.** 100배로 자주 들어가면 수수료가
  손익보다 커지는 구간이 있고 무기한은 8시간마다 펀딩을 낸다. 수수료를 빼놓고
  "오늘 얼마 잃었나"에 답하면 그건 다른 질문의 답이다
- **하루 경계는 UTC 자정.** 거래소 기록과 펀딩 정산이 그 기준이라, 한국 시간
  자정으로 자르면 같은 펀딩이 두 번 세지거나 빠진다
- **못 읽으면 막는다.** 오늘 손익을 모르면 한도를 넘었는지 알 수 없다 —
  "조회가 실패했으니 일단 보내자"는 한도를 안 건 것과 같다
- **청산에는 안 건다.** 한도에 걸렸다고 나갈 수 없게 만들면 그건 손실을
  키우는 잠금이고, 잠금의 목적과 정반대다
- **모든 시장에 건다.** 한도는 계좌 단위이지 시장 단위가 아니다 — 선물에서
  채운 뒤 현물로 옮겨 계속하면 잠근 의미가 없다
- 두 한도가 다 있으면 **작은 쪽**이 이긴다. 둘을 걸어 둔 사람은 둘 다 지키고
  싶은 것이다
- **이익 목표에서도 멈출 수 있다**(선택). 되돌려주는 날은 대개 목표를 넘긴
  뒤에 나온다
- 기본값은 `DAILY_MAX_LOSS_PCT=5`. 한도 없이 자동매매가 도는 상태를 기본으로
  두지 않는다

### 보이지 않으면 없는 것과 같다

주문 버튼을 눌렀을 때 처음 알게 되면, 그 시점까지 한도를 모른 채로 계획을
세운 것이다. 주문판에 **오늘 남은 여유**를 상시 표시한다. 상태는 셋으로
구분한다 — `여유 40.00 USDT` / `잠김 — 신규 진입 불가` / `확인 불가`.
'여유 있음'과 '모름'이 같아 보이면 안 된다.

### 걸린 경로

`preflight`(미리보기) · `binance/futures/order`(수동) · `daily-ladder`(자동) ·
`webhook/tradingview` · `paper/order`(모의). 체크리스트 항목이라 이 경로들이
공통으로 받는다.

Gate는 원장 엔드포인트가 달라(`account_book`, 초 단위 시각, `pnl/fee/fund`)
읽는 쪽만 갈랐다. 합산·판정은 같은 순수 함수 하나가 두 거래소를 처리한다 —
한도 판정이 거래소마다 갈리면 한쪽만 고쳐진다. ⚠ Gate 원장 호출은 실계좌로
검증되지 않았다.

## 모의 / 테스트넷 / 실전 (완료)

`lib/markets/tradeMode.ts`(순수, 테스트 17개) + `lib/engine/paperPlan.ts`(순수, 테스트 14개)
+ `api/paper/{account,order,close}` + `TradeModeSwitch`·`PaperWallet` + 주문판 배선.

### 먼저 드러난 것: 신규 진입이 전부 막혀 있었다

USDⓈ-M 주문판에 **손절 입력 칸이 없었다.** 서버(manualPlan)는 손절 없는 진입을
거부하는데 화면이 그 값을 보내지 않았으니, 이 경로의 신규 진입은 100% 거부다.
화면에서 못 넣는 값을 서버가 요구하면 그 기능은 죽은 것이다. 손절 % 줄을 넣고
`stopLossPct`를 보내게 했다. 결과 손절가와 "청산 거리보다 먼가"도 같이 보여준다.

### 세 모드

| 모드 | 주문이 가는 곳 | 연결 | 돈 |
|---|---|---|---|
| 모의 | `/api/paper/order` — 앱 안의 장부 | 필요 없음 | 가상 |
| 테스트넷 | 거래소 테스트넷 | `is_testnet !== false`인 연결 | 없음 |
| 실전 | 거래소 실계좌 | `is_testnet === false`인 연결 | **진짜** |

- **기본은 모의다.** 새로 열었을 때 실전에 서 있으면 안 된다
- **실전은 기억하지 않는다.** 어제 실전으로 켜뒀다는 이유로 오늘 첫 화면이
  실전이면 안 된다 — 모드 저장에서 LIVE만 뺀다
- 실전 전환에 확인, **주문할 때도 다시 확인**한다. 전환은 몇 분 전 일이고
  그 사이 화면을 여러 번 만졌다
- `is_testnet`이 null이면 **실전이 아니다.** 설정이 덜 된 계정이 실계좌가
  되면 안 된다 (프로젝트 전체가 쓰는 규칙과 같다)
- 실전 연결을 고른 채 테스트넷 탭을 누르면 연결을 바꾸되 **그 사실을 말한다.**
  조용히 바꾸면 사용자는 자기가 고른 계정으로 나간다고 믿는다

### 모의에도 같은 규칙을 건다

손절 필수·배율 상한·손절 방향·청산거리·증거금. 규칙이 실전과 다르면 그 연습은
쓸모가 없다 — 손절 없이 100배로 넣어도 되는 화면에서 익힌 습관이 실계좌에서
그대로 나온다.

다른 것은 하나뿐: **충전할 수 있다.** 다만 넣은 금액은 초기자본에도 더해진다.
안 그러면 넣은 돈이 수익으로 잡혀 수익률이 부풀려지고, 그 성적표는 아무것도
말해주지 않는다. 화면에도 그렇게 적어 뒀다.

체결가는 **서버가 받는다.** 화면이 보낸 가격을 쓰면 유리한 값을 넣어 장부를
만들 수 있고, 그러면 성적표가 의미를 잃는다.

### 이 과정에서 고친 것

- `/api/paper/positions`가 **쿼리스트링의 userId를 그대로 믿고 있었다.**
  남의 가상 계좌를 URL만 바꿔 들여다볼 수 있었다. 인증 헤더에서만 얻게 했다
- `canOpenFutures`가 `tree.futures.ok`를 바로 읽어서, `/api/wallets`가 다른
  모양을 주면 **터미널 전체가 흰 화면**이 됐다. 브라우저 확인 중에 실제로
  터뜨려 보고 알았다. 증거금을 모르는 것과 화면이 사라지는 것은 다른 문제다
- 모의 포지션 카드를 실계좌 카드와 **따로 뒀다.** 같은 컴포넌트에 조건을
  붙이면 언젠가 모의 카드의 청산 버튼이 실계좌 라우트를 부른다

### 브라우저에서 확인한 것

기본 모드가 모의 · 충전 요청(`{action:'deposit',amount:5000}`) · 모의 주문이
`/api/paper/order`로 가고 **실계좌 라우트는 호출되지 않음** · 테스트넷 전환 후
`connectionId:'t1'`(테스트넷 연결) · 실전 전환 확인창 + 주문 확인창 + 
`connectionId:'l1'`(실전 연결) · 세 경우 모두 `stopLossPct`가 실린다.

## 포지션 카드 — 뒤집기 · TP/SL (완료)

`api/binance/futures/reverse` + `BottomDock`의 `ReversePanel`·`TpSlPanel`.

### 뒤집기를 라우트로 둔 이유

화면에서 청산·진입을 두 번 부르면 될 것 같지만 안 된다. 그 사이에 **아무도
확인하지 않는 순간**이 생긴다:

- 청산 응답을 못 받았는데 진입을 보내면, 청산이 됐든 안 됐든 반대 주문이
  나간다. 안 됐다면 그건 뒤집기가 아니라 **양방향 포지션**이다
- 화면이 닫히거나 네트워크가 끊기면 청산만 되고 진입은 사라진다

그래서 서버가 순서를 쥔다. **청산이 끝난 것을 거래소에 다시 물어 확인한
뒤에만** 반대 진입을 보낸다(0.7초 간격 4회, 원래 수량의 1% 이하면 닫힌 것으로
본다). 확인하지 못하면 진입하지 않는다 — 결과는 '평평한 계좌 + 이유'이고,
그건 되돌릴 수 있는 상태다.

주문 자체는 이 라우트가 만들지 않는다. 두 단계 모두 **기존 수동 주문
라우트(`../order`)를 그대로 부른다.** 거기에 킬스위치 가드·거래 전 점검·
수동 계획 검사·멱등 키·ISOLATED 강제·손절 부착·UNKNOWN 처리가 다 있다.
여기서 executeOrder를 직접 부르면 관문이 두 벌이 되고, 그러면 한쪽만 고쳐진다.

### 정한 것

- **손절 폭 없이는 뒤집지 않는다.** 없으면 진입이 거부되는데, 그때는 이미
  청산이 끝난 뒤다 — 포지션이 사라진 채로 끝난다. 서버와 화면 양쪽에서 막는다
- 손절가는 **뒤집힌 방향 기준**으로 계산한다. 지금 방향으로 계산하면 걸자마자
  발동하는 값이 나온다
- 화면이 본 방향·수량을 같이 보낸다. 화면 값은 최대 10초 전 것이라 그 사이
  손절이 걸렸을 수 있다. 방향이 다르거나 수량이 5% 넘게 다르면 거부한다
- TP/SL은 **가격으로** 보낸다. %로 보내고 서버가 다시 계산하면 화면에 보여준
  값과 다른 주문이 걸린다. 방향이 틀린 값(LONG 익절이 현재가 아래)은 화면에서
  막는다 — 걸자마자 체결되는데 화면에는 '설정됨'으로 보인다

### 브라우저에서 잡은 것

`Field` 컴포넌트를 `TpSlPanel` **안에** 정의해 뒀었다. 부모가 다시 그려질
때마다 새로운 타입이 되어 React가 input을 갈아 끼우고, **한 글자 칠 때마다
포커스가 빠진다.** 자동 조작(`fill`)은 값을 한 번에 넣어서 안 보였고, 한
글자씩 치게 하니 드러났다. 컴포넌트를 밖으로 뺐다.

성공 문구가 판과 함께 사라지는 것도 그때 봤다. 결과를 카드로 올렸다 —
뒤집기는 무슨 일이 일어났는지 모르면 안 되는 동작이다.

## 모바일 세로 배치 — 한 줄 스크롤 (완료)

`MobileShell.tsx` + `BottomDock`의 `flow` 모드 + `OrderPane`의 호가 줄 높이.

### 무엇이 문제였나

세로 화면을 세 칸(주문·호가 / 포지션 27vh / 차트)으로 잘라 각 칸이 자기 안에서
스크롤했다. 둘이 망가졌다:

1. **포지션 카드 하나가 27vh에 안 들어간다.** 청산가와 청산 버튼이 항상 칸
   밖에 있어서, 들고 있는 것을 보려면 좁은 칸을 또 스크롤해야 했다
2. **스크롤이 두 겹**이라 손가락이 어디에 닿았는지에 따라 화면이 다르게
   움직인다 — 어느 칸을 만지는지 보이지 않으므로 예측이 안 된다

### 어떻게 바꿨나

페이지가 스크롤을 갖는다. 첫 화면은 주문+호가, 끌어내리면 포지션이 화면을
채운다. 헤더와 탭 줄은 고정이라 어디까지 내려가도 종목·가격·STOP과 지금 보는
탭이 보인다.

- 헤더 높이는 **재서** 쓴다. 종목 이름이 길거나 시장 전환 줄이 접히면 높이가
  달라지는데, 상수로 박으면 그때 탭 줄이 헤더 밑에 겹친다
- 스크롤 통의 높이도 잰다. 이 화면은 앱 탭 안에 끼워지기도 해서(embedded)
  `100dvh`가 통 높이와 다르다
- 스냅은 `proximity`. `mandatory`면 카드를 읽는 중에 화면이 끌려간다
- 주문+호가 칸은 **화면에 맞춰 늘리지 않는다.** 늘리면 주문폼 가운데에 죽은
  여백이 200px 넘게 생긴다 (실제로 그렇게 만들어 보고 알았다)

### 재 보고 알게 된 것: 호가 한 줄이 44px이었다

`globals.css`에 `button { min-height: 44px }`(터치 목표 최소 크기)가 있어서
**호가 줄도 44px**이었다. 위아래 7줄이면 616px — 세로 화면 하나가 호가판
하나로 다 찬다. 주문폼·호가·포지션을 한 화면에서 같이 보는 것이 불가능했고,
깊이를 보려면 또 스크롤해야 했다.

호가는 낱개 버튼이 아니라 **사다리**다. 줄 하나를 크게 만드는 대신 줄이 여러
개 보이는 것이 이 판의 목적이라, 여기서만 그 규칙을 깼다(21px). 실제 거래소
앱들도 20px 안팎이다. 브라우저에서 재서 확인했다: 44px → 21px, 호가판 높이
807px → 477px.

### 떠 있는 현재가를 눌러도 그 가격이 들어간다

호가 줄만 눌렸고, **가장 크게 떠 있는 가운데 현재가가 유일하게 안 눌리는
것**이었다. 지정가를 넣을 때 제일 자주 쓰는 값이 현재가라 그게 제일 이상했다.

같이 고친 것: 같은 가격을 두 번 누르면 아무 일도 일어나지 않았다. 주문폼이
`useEffect([presetPrice])`로 값을 받는데 값이 안 바뀌면 effect가 안 돌기
때문이다. 그 사이 가격을 직접 고쳐 놨다면 되돌릴 방법이 그 줄을 누르는
것뿐인데 그게 안 들었다. 누른 **횟수**를 같이 들고 다니게 했다(`usePickedPrice`).

브라우저에서 확인한 것(호가 스트림을 가짜로 채워서 — 이 컨테이너는 프록시가
Binance를 막는다): 현재가 누름 → 지정가 전환 + 64850 채워짐 · 999로 직접 고친
뒤 같은 현재가 다시 누름 → 64850으로 돌아옴 · 호가 줄 누름 → 그 줄 가격.

## 운영자 계정 승급 (완료)

`lib/auth/promotionMap.ts`(순수, 테스트 16개) + `ensureAdmin.ts` + `.env.example`
+ `migrations/021_grant_owner_role.sql`.

예전에는 `ADMIN_EMAILS` 하나였고 언제나 `admin`만 줬다. 그런데 등급은
admin(4) < developer(5) < super_admin(6) 순서라 `/developer`는 developer
이상이라야 열린다 — **소유자 본인도 환경변수만으로는 `/developer`에 못
들어갔다.** DB에서 직접 UPDATE하는 방법밖에 없었고 그건 주석에만 적혀 있었다.

- 목록을 셋으로 나눴다: `ADMIN_EMAILS` · `DEVELOPER_EMAILS` · `SUPER_ADMIN_EMAILS`
- 한 이메일이 여러 목록에 있으면 **가장 높은 등급**. 읽는 순서에 좌우되면 안 된다
- **올리기만 한다.** 환경변수를 잘못 고쳐서 super_admin이 admin으로 내려가면
  안 된다. 모르는 등급 이름도 건드리지 않는다 — 낮은 것으로 보고 덮으면 강등이다
- 판정은 순수 함수라 테스트가 붙는다. 여기서 틀리면 결과가 권한 상승 또는
  강등인데, 화면에서는 둘 다 '관리자입니다'로 똑같이 보인다

021 SQL에 **이메일을 박아 두지 않았다.** 그러면 이 저장소를 배포하는 누구나
그 계정을 관리자로 만들게 된다 — 권한 규칙이 코드에 상수로 남으면 뒷문이다.
실행할 때 채워 넣는 스크립트로 뒀다.

## COIN-M 손절 부착 (완료)

`markets/coinM.ts`의 `coinMStopPlan`(순수) + `exchanges/binanceCoinFutures.ts` 보강 +
`api/binance/coinm/order` + `components/terminal/CoinMOrderPanel.tsx`.

### 무엇이 문제였나

COIN-M 주문 경로는 **손절을 아예 붙이지 않았다.** 포지션 크기는 손절이 있다는
전제로 계산되는데, 역방향 계약에서는 그 전제가 존재하지 않았다.

그래서 거래 전 점검의 `STOP_ATTACHED`·`LIQUIDATION_DISTANCE`가 `markets: ['USDM']`
이었다. 물려 놓으면 COIN-M 주문이 통째로 막혔기 때문이다 — 면제가 아니라
**못 고친 위험의 표시**였고, 그 자리에 "붙이면 배열을 넓히고 테스트를 뒤집을 것"을
적어 뒀다. 이번에 그대로 했다.

### 정한 것

- **진입에 손절가가 없으면 거래소를 건드리기 전에 거부한다.** 마진 모드·배율
  설정보다 앞에 둔다 — 거부될 주문 때문에 계좌 설정을 바꿀 이유가 없다
- **방향은 순수 함수로 판정한다** (`coinMStopPlan`). BUY 진입 → SELL 손절 ·
  마크가보다 아래. 여기서 부호를 틀리면 손절이 걸자마자 발동하거나 영원히
  발동하지 않고, 둘 다 화면에는 '손절 걸림'으로 보인다
- **`closePosition: true`로 건다.** 계약 수를 적어 두면 그 수량만 닫히고 나머지는
  보호 없이 남는다. 트리거는 `workingType: MARK_PRICE` — 마지막 체결가로
  트리거하면 얇은 호가에서 잘못 터진다
- **손절을 못 걸면 방금 연 포지션을 즉시 닫는다** (감사 지적 3번 · USDⓈ-M·Gate와
  같은 규칙). 되돌리기까지 실패하면 그 사실을 그대로 돌려준다 — 여기서 ok:true를
  주면 화면에는 '진입 성공'이 뜨고 보호 없는 역방향 포지션이 남는다
- **체결 후에 실제 청산가로 손절 위치를 다시 본다.** 주문 전 판정은 추정값을 썼고,
  실제 청산가는 그보다 가깝다. 손절이 실제 청산 너머면 그 손절은 작동할 기회가
  없으므로 포지션을 되돌린다
- **되돌릴 수량은 거래소에서 읽은 현재 계약 수다.** 우리가 보낸 수량을 쓰면 부분
  체결이었을 때 없는 수량까지 닫으려 해 거부된다

### 신규 진입의 청산가 문제

포지션이 없으면 거래소가 청산가를 주지 않는다(0). 그대로 넘기면 청산거리 검사가
`unknown`이 되어 **모든 첫 COIN-M 주문이 막힌다** — USDⓈ-M에서 마진 모드로 겪은
것과 같은 함정이다.

그래서 `inverseLiquidationPrice`의 추정값을 쓴다. 다만 그 추정은 실제보다 **멀게**
나오므로(함수 주석에 그렇게 적혀 있다) 그대로 쓰면 검사가 느슨해진다. 유지증거금률을
넉넉히(1%) 잡아 청산가를 진입가 쪽으로 당겼다 — **틀리더라도 더 엄격한 쪽으로**
틀리게. 그리고 체결 후에는 실제 값으로 다시 본다(위 항목).

### 화면도 같이 고쳤다

`CoinMOrderPanel`은 손절을 보내지 않았다. 서버가 이제 손절 없는 진입을 거부하므로,
그대로 두면 **COIN-M 주문 버튼이 죽는다.** 손절 폭 선택(1·2·3·5·10%, 기본 2%)을
넣고 **결과 가격을 화면에 적는다** — 기본값이 있어도 숨겨지지 않게.

배율이 높으면 청산가가 가까워진다(100배면 1% 남짓). 그때 2% 손절은 청산 너머다.
그 조합에서는 버튼을 막고 이유를 적는다 — 서버도 같은 것을 막지만, 왕복 전에
말해 주는 편이 낫다. 퍼센트가 아니라 **계산된 가격**을 보낸다: 서버가 자기
마크가로 다시 계산하면 화면에 보여준 값과 다른 손절이 걸린다.

### 이 과정에서 드러난 것

손절 주문 이름을 `${clientOrderId}SL`.slice(0,36)로 만들면 원본이 길 때 'SL'이
잘려 나가 **진입 주문과 같은 이름**이 된다. 거래소는 중복 주문으로 거부하고 화면에는
'손절 실패'만 남는다 — Gate에서 겪은 것과 같은 함정이라 앞을 자르도록 바꿨다.

`getCoinMPositions`는 `contracts !== 0`으로 걸러낸다. 신규 진입 심볼이 목록에 없어
마진 모드·배율·청산가를 확인할 수 없으므로 `getCoinMSymbolRisk`(수량 0도 돌려준다)를
따로 만들었다 — USDⓈ-M의 `getSymbolPositionRisk`와 같은 이유다.

**킬스위치는 여전히 COIN-M을 건드리지 않는다** (`executeKillActions`는 fapi만
부른다). 미처리 과제 2번으로 올렸다.

## Gate 거래소 경로 (완료)

`lib/exchanges/gatePlan.ts`(순수, 테스트 37개) + `gateFutures.ts` 보강 +
`orderExecutor.ts`의 Gate 분기 재작성.

### 무엇이 문제였나

바이낸스 경로에는 감사 지적을 받아 채워 넣은 보호가 다섯 겹 있었는데, Gate 분기는
그중 **하나도** 갖고 있지 않았다. 실제 돈이 걸린 경로다.

| Gate에 없던 것 | 결과 |
|---|---|
| 마진 모드 확인 | 교차 계좌에서 한 종목 손실이 지갑 전체로 번진다 |
| `setLeverageGateFutures` 결과 확인 | 반환값을 **버렸다**. 실패해도 주문이 나가고, 계좌에 설정된 배율이 무엇이든 그대로 열린다 |
| 손절 부착 | 포지션 크기는 손절이 있다는 전제로 계산됐는데, 그 전제가 Gate에는 없었다 |
| 수량 단위 | `Math.round`. 0.4 → 0(주문 소멸), 1.6 → 2(의도보다 25% 큰 포지션) |
| 체결 확인 | ioc 주문이 하나도 못 붙어도 `finished`가 오면 '접수'로 보고 |

### 정한 것

- **레버리지 0은 교차 마진이다.** Gate v4의 규칙이라 0을 '배율 0'으로 기록하지
  않는다 — 배율은 그때 *모르는* 값이다. 설정 후 포지션을 **되읽어** 격리를 확인하고,
  확인하지 못하면 주문을 중단한다. "200을 받았다"와 "계좌가 격리다"는 다른 얘기다
- **수량은 내림한다.** 올리면 의도보다 큰 포지션이 열린다. 내려서 1계약 미만이면
  주문하지 않는다
- **손절 방향을 보내기 전에 검사한다.** LONG은 `rule:2`/`close_long`,
  SHORT은 `rule:1`/`close_short`. 뒤집히면 손절이 걸자마자 발동한다 — 그것도
  화면에는 '손절 걸림'으로 보인다
- **손절을 못 걸면 방금 연 포지션을 즉시 닫는다** (감사 3번과 같은 규칙).
  이게 아래 '검증되지 않은 것'을 감당하는 장치다
- **중복 확인에 실패하면 보내지 않는다.** 예전에는 조회 실패와 '주문 없음'이 둘 다
  `null`이었다. 호출자는 그것을 '중복 아님'으로 읽고 주문을 보냈으므로,
  레이트리밋 한 번이 중복 체결이 될 수 있었다
- **주문 이름(text)이 겹치지 않게 한다.** 특수문자를 지우고 28자에서 자르기만 하면
  `MF/1:2`와 `MF12`가 같은 `t-MF12`가 된다. 이름이 겹치면 중복 확인이 남의 주문을
  내 주문으로 읽고 "이미 주문돼 있습니다"로 끝난다 — 주문하지 않은 채 성공 보고다.
  값이 바뀔 때만 원문 해시 6자를 붙인다 (`LD…`·`MF…` 같은 현재 키는 그대로)
- **체결 0을 접수로 적지 않는다.** 시장가는 `tif:'ioc'`라 유동성이 없으면 Gate가
  200 + `finished` + `left=요청수량`을 준다. 그걸 ACKED로 적으면 없는 포지션에
  손절을 걸려 하고 엉뚱한 이유가 화면에 뜬다. `left`를 못 읽으면 **모르는 것**이지
  미체결이 아니다

### 거래소별로 갈라 읽는다

거래 전 점검·상태 대조가 **전부 바이낸스 API를 부르고 있었다.** Gate 연결에서는
Gate 키로 바이낸스에 서명해 보내는 셈이라 모든 조회가 실패하고, 필수 항목이
`unknown`이 되어 **모든 Gate 주문이 막혔다.** 안전하게 틀리기는 했지만 화면에는
'확인 불가'만 남는다.

갈라 준 곳: `reconcileCheck.gatherAndReconcile` · `preflight.collectChecklistInput` ·
`api/autotrade/daily-ladder` · `api/webhook/tradingview` · `reconcilePendingOrders`.
시각도 Gate 호스트(`/api/v4/spot/time`)에 따로 묻는다 — 다른 회사 서버와 맞는지를
재면 그건 다른 것을 재는 것이다.

변환은 `gatePositionToRisk` 한 곳에만 둔다. 'leverage 0은 교차'를 호출자마다
다시 적으면 네 벌이 되고, 한 곳만 고쳤을 때 나머지 세 곳이 조용히 틀린 채 남는다.

### 이 과정에서 드러난 별개 버그

**TradingView 웹훅이 `exchange: 'binance'`를 하드코딩**하고 있었다. 연결은 id로만
찾으므로 Gate 연결이 그대로 들어오는데, 그러면 점검용 조회는 전부 실패하고
통과했다면 엉뚱한 거래소로 주문이 나갔다. 이제 연결에서 읽는다.

`reconcilePendingOrders`의 Gate 분기에는 **포지션 교차 확인이 없었다** — '주문이
안 보임'만으로 판단했다. 반영이 늦는 거래소에서 그건 중복 주문의 근거가 된다.
`getPositionsGateFutures`로 바이낸스와 같은 교차 확인을 넣었다.
계약 이름도 그 자리에서 `replace('USDT','_USDT')`를 직접 하고 있었다 → `toGateContract`.

### 검증되지 않은 것 (읽는 사람이 알아야 하는 것)

**`placeStopGateFutures`는 실계좌 응답을 본 적이 없다.** 문서만 보고 만든 요청이다
(`price_orders` · `initial.size:0` + `auto_size` · `price_type:1` 마크 가격).
이 컨테이너에서는 프록시가 `gateio.ws`·`gateapi.io`를 막아 공개 엔드포인트조차
찍어볼 수 없었다. 확인한 것은 **나가는 HTTP의 모양**이다 — fetch를 가로채
경로·메서드·쿼리·본문·서명 헤더를 눈으로 봤다.

그래서 손절 실패 시 되돌리기가 있다. API 모양이 틀렸더라도 결과는 '보호 없는
포지션'이 아니라 '포지션 없음 + 실패 보고'다. 다만 **항상 되돌려지는 상태는
정상이 아니다** — 테스트넷에서 한 번은 직접 확인해야 한다 (미처리 과제 1번).

## 전역 단축키 (완료)

`lib/commands/keymap.ts`(순수) + `useHotkeys.ts`(리스너) + `ShortcutHelp.tsx`. 테스트 42개.

팔레트와 **같은 레지스트리**를 쓴다. 키를 핸들러에 박지 않는다 — 창구가 여럿이 되면
창구마다 위험 등급을 판단하게 되고, 그중 하나는 반드시 빼먹는다.

### 이 기능에서 가장 중요한 것: 입력창에서는 터지지 않는다

`Shift+B`를 시장가 매수에 걸어 두면, 수량칸에 숫자를 치다가 대문자 B가 들어가는
순간 주문이 나간다. 메모칸에 'BTC 봐야 함'을 적는 것도 같은 결과다. 그래서
`shouldIgnoreTarget`이 INPUT·TEXTAREA·SELECT·contenteditable을 **조건 없이** 무시한다.

체크박스나 range 같은 "글자를 안 받는 input"도 무시한다. 예외 목록을 만들고 싶어지지만,
그 목록이 틀리는 순간 조용히 주문이 나간다. 단축키 하나 못 쓰는 불편과 잘못된 주문은
비교 대상이 아니다.

### 실린 단축키 (10개)

| 키 | 동작 |
|---|---|
| `Ctrl/⌘ + K` | 빠른 명령 팔레트 |
| `?` | 단축키 도움말 |
| `G` 다음 `M` / `N` / `C` / `P` / `A` / `T` / `R` / `S` | 시장 / 뉴스 / 캘린더 / 포트폴리오 / 자동매매 / Pro 터미널 / 리스크 / 설정 |

**주문 단축키는 없다.** `Shift+B`(시장가 풀매수)를 요청받았지만 레지스트리에 주문
커맨드가 없다. 없는 커맨드에 키를 걸면 눌러도 아무 일이 없고, 그건 고장인데 화면에서는
구분되지 않는다. `Binding.trading` 표시와 `enableTrading`(기본 **꺼짐**) 스위치를 만들어
뒀으니, 주문 커맨드를 배선할 때 `trading: true`로 추가하면 된다.

`g`로 시작하는 두 단계 조합을 쓰는 이유: 한 글자 단축키는 개수가 금방 떨어지고 오타로
눌리기 쉽다. 이동은 급할 일이 없으니 두 번 누르는 편이 낫다.

### 정한 것

- **ctrl과 meta를 `mod` 하나로 합친다.** 맥·윈도우에 각각 바인딩을 두면 한쪽만 고치는
  일이 생기고, 사용자는 자기 OS에서만 안 되는 것을 본다
- **shift는 글자·이름있는 키에만 붙인다.** `Shift+/`는 브라우저가 `?`를 주는데 여기에
  `shift+`를 붙이면 `shift+?`가 된다 — 사용자가 기대하는 이름은 `?`다
- **시퀀스와 반복을 같은 방식으로** 다룬다. `g m`과 `esc esc esc`는 "정해진 순서가 제한
  시간 안에 들어왔는가"라는 같은 문제다. 특수 처리를 따로 두면 한쪽에만 버그가 생긴다
- **엉뚱한 키를 눌렀다가 다시 치면 먹는다.** `g` → `x` → `g` → `m`이 동작한다.
  버퍼만 비우면 그 `g`가 씹혀 사용자는 단축키가 안 먹는다고 느낀다
- `guarded`는 제한 시간(4초) 안에 **같은 단축키를 다시** 눌러야 실행된다. 팔레트의
  'Enter 두 번'과 같은 규칙이고, 판단은 `needsConfirm` 하나만 본다
- 확인 대기 중임을 **알림으로 알린다.** 화면에 아무 표시가 없으면 사용자는 씹혔다고
  생각하고 다시 누르는데, 그 두 번째가 실행이 된다
- 실행 후 확인 상태를 비운다 — 안 비우면 세 번째 입력이 또 실행된다
- 팔레트·도움말이 열려 있으면 전역 단축키를 끈다. 두 곳이 동시에 반응하면 안 된다
- 도움말은 `DEFAULT_BINDINGS`를 그대로 그린다. 목록을 두 곳에 적으면 언젠가 한 곳만
  고치고, 그러면 도움말이 거짓말을 한다

테스트가 잡아 주는 것 둘: **같은 순서에 두 커맨드를 걸면 실패**(하나가 죽는다),
**한 단축키가 다른 단축키의 앞부분이면 실패**(앞의 것이 절대 실행되지 않는다).
그리고 바인딩이 가리키는 커맨드 id가 실제로 존재하는지도 본다.

### 실제로 확인한 것

키 입력을 순서대로 흘려 넣어 확인:
```
g → 대기…    m → 실행: 시장 보기로
g → 대기…    x → 없음    g → 대기…    c → 실행: 경제캘린더로
mod+k → 실행: 빠른 명령 팔레트
? → 실행: 단축키 도움말
shift+b → 없음 (주문 단축키 미배선)

입력칸 안에서: "g" in <INPUT> → 무시 · "b" in <INPUT type=number> → 무시
              "b" in contenteditable → 무시

guarded: 1회차 ask → 2회차(0.8초 후) run → 3회차 ask(상태 비워짐) → 한참 뒤 ask
```

## 관리자 화면 차단 (완료)

미들웨어가 `/admin`·`/developer`를 **서버에서** 막는다. 테스트 39개.

### 무엇이 문제였나

`middleware.ts`는 아무것도 막지 않았고, 파일에 그 이유가 주석으로 적혀 있었다 —
세션이 localStorage에 있어 미들웨어가 볼 수 없다는 것. 사실이었지만 두 가지가 남았다.

1. **`/developer`의 문지기가 위조 가능했다.** `getMockSession()`으로 localStorage의
   `tg_mock_session_v2`를 읽어 role을 봤다. 콘솔에서 그 JSON의 role만 바꾸면 화면이
   열렸다 — 문지기가 방문자 주머니에 들어 있던 셈이다
2. 화면은 열리고 데이터만 403이라 **"잠긴 줄 알았는데 안 잠긴"** 상태였다

### 어떻게 고쳤나

세션 전체를 쿠키로 옮기지 않았다. 그러면 저장소 어댑터·refresh token·4KB 조각내기까지
로그인 흐름 전체를 건드린다. 대신 **access token만 쿠키(`tg_at`)로 복사**했다.
원본은 그대로 localStorage에 있고 로그인·로그아웃 코드는 손대지 않았다.

| 파일 | 역할 |
|---|---|
| `lib/auth/adminGate.ts` | 통과 판정 (순수 함수 + 조회). 테스트가 붙는 자리 |
| `middleware.ts` | 쿠키를 꺼내 판정에 넘기고 리다이렉트만 한다 |
| `components/auth/SessionCookieSync.tsx` | 세션 토큰 → 쿠키 복사. 레이아웃에 상주 |
| `lib/auth/clientRole.ts` | 화면이 '내 role'을 묻는 하나의 경로 |

핵심 규칙:
- **role을 토큰에서 읽지 않는다.** JWT payload는 서명 없이 누구나 만든다 —
  `{"role":"super_admin"}`은 base64 한 번이다. 디코드해 쓰는 값은 `sub`·`exp` 둘뿐이고
  권한은 Supabase가 서명을 검증한 뒤 돌려준 role로만 정한다
- **PostgREST 한 번으로 서명 검증과 권한 확인을 같이 한다.** 서명이 틀리면 401이고,
  RLS(`profiles_self_read`)가 걸려 있어 남의 행은 애초에 안 읽힌다. service role 키를
  미들웨어에 들고 다니지 않는다
- **조회에 실패하면 통과시키지 않는다.** 정전에 풀리는 잠금장치는 잠금장치가 아니다.
  대신 관리자가 잠깐 못 들어오는 쪽을 택했다 (화면 2개고 API는 따로 막혀 있다)
- **모르는 role 문자열은 막는다.** '일단 통과'를 택하면 역할 하나 추가할 때 문이 열린다
- `/developer`는 `/admin`보다 등급이 높다 — admin 계정은 /developer에 못 들어간다.
  기존 `canAccessDeveloper()`와 같은 기준이다. 두 곳이 다르면 화면과 미들웨어가
  서로 다른 말을 한다

같이 고친 것:
- `/admin`이 `role !== 'admin'`으로 판정해 **developer·super_admin을 막고 있었다.**
  등급이 더 높은데 그 화면만 다른 말을 했다 → `canAccessAdmin()` 하나로 통일
- `/admin`과 `/developer`가 같은 질문에 각자 다른 방법으로 답하고 있었다 →
  `clientRole.ts` 하나로 모음
- `/unauthorized`가 거절 사유를 표시한다. `lookup_failed`는 권한 문제가 아니라
  서버가 확인을 못 한 것이라 '다시 시도'가 답인데, "권한 없음"만 보이면 계정을 의심한다

### 실제로 확인한 것

프로덕션 빌드를 띄우고 curl로 (전부 307 리다이렉트):

| 요청 | 결과 |
|---|---|
| 쿠키 없이 `/admin` | `/auth?next=%2Fadmin&reason=no_token` |
| 쿠키 없이 `/developer` | `/auth?next=%2Fdeveloper&reason=no_token` |
| `/admin/anything` (하위 경로) | `/auth?next=%2Fadmin%2Fanything&reason=no_token` |
| 쓰레기 쿠키 | `reason=bad_token` |
| 만료 토큰 | `reason=expired` (네트워크 안 부름) |
| **payload에 `role:super_admin`을 심은 위조 토큰** | `reason=unverified` — 거부됨 |
| `/terminal` (보호 대상 아님) | 200, 그대로 통과 |

**아직 확인 못 한 것**: 실제 관리자 계정으로 통과하는 경로. 이 컨테이너에 진짜 anon
키가 없어 유닛 테스트(가짜 PostgREST)로만 검증했다. 배포 후 관리자로 `/admin`에
들어가 보는 것이 남았다 — 만약 막히면 `/unauthorized`의 **사유 코드**를 보면 된다
(`lookup_failed`면 조회 문제, `insufficient_role`이면 DB의 role 문제다).

**한 번 튕길 수 있다**: 배포 직후 첫 방문은 쿠키가 아직 없어 `/auth`로 갔다가
되돌아온다. `/auth`가 `next` 파라미터로 원래 목적지에 다시 보내고, 그때는 쿠키가
생겨 있어 통과한다. `next`는 같은 사이트 경로만 따라간다 — 로그인 화면이
오픈 리다이렉트가 되면 피싱에 그대로 쓰인다.

**이 장치의 한계**: 미들웨어는 페이지 요청만 막는다. JS 번들 파일 자체는 여전히
공개 URL이다. 코드를 숨기는 장치가 아니라 화면에 들어가지 못하게 하는 장치이고,
데이터의 방어선은 계속 API의 `requireAdmin()`이다.
